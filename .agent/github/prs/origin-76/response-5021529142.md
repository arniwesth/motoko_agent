---
repo: arniwesth/motoko_agent
pr: 76
in_reply_to: 5021529142
---

Checked this now that #81 has merged (2026-08-11), against your three conditions.

**SYSTEM_MD forwarding — covered.** `src/tui/src/config.ts:30` maps `agent.system_prompt` to the
`SYSTEM_MD` env var, and `index.ts` carries the workdir-relative path contract.

**Materialization — covered.** `src/tui/src/system-prompt.ts` is now a dedicated module:
`systemPromptForWorkspace` resolves the configured path against the workdir and refuses anything
that escapes it, and `materializeSystemPromptArg` copies external content into a managed
in-workspace file.

**`system_prompt_built` observability — not covered.** On `main_dst` the identifier appears only in
design documents — `001_DST/ADR-001`, `004_phase_core_refactor/ADR-002`, and two research notes
under `.agent/research/DST/`. There is no occurrence anywhere under `src/`. It is specified, not
implemented.

So your close condition isn't met, and by your own argument the missing third is the one that
matters: you diagnosed the ailang v0.30.0 compat break *through* this PR's observability events,
which is precisely the case where a silently-absent system prompt costs days. The forwarding and
materialization fixes are redundant with #81 now; the observability is not.

Suggest keeping #76 open but narrowing it to the `system_prompt_built` event alone, so it stops
carrying a diff that #81 already landed.
