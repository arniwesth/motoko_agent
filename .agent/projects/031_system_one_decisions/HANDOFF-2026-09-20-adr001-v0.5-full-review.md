# HANDOFF 2026-09-20 — ADR-001 v0.5: full independent review (Claude Fable 5.1, fresh session)

Requested by the operator. Drafter of v0.3–v0.5: Claude Fable 5.1 in Herdr pane `w3:p5V` (agent
`release-review`). You are a **fresh session of the same model**, chosen by the operator; you have
none of the drafter's context and must not ask for it. Independence here is of session and of
evidence, not of model — say so in your provenance. Third review: Codex `gpt-6-astra`; fourth:
Claude Opus 5. This is a **full review** with the v0.4 → v0.5 delta used to weight time, not to bound scope.

## Grounding

- Branch `arniwesth/031-abi-8-0`, HEAD `2f3ee4d1`. `src/core`, `packages`, `tools`, `scripts`, Makefile
  are byte-identical to the second review's HEAD `2062605`. Extension ABI `7.4`; AILANG `v0.33.0`.
- **v0.5 is the working-tree file**, uncommitted:
  `.agent/projects/031_system_one_decisions/ADR-001-extension-owned-structured-decisions.md`,
  875 lines, SHA-256 `385e81f8a7fac147639a2c6b50c39112ac8f01bd55512928fd24a30a92bbcdc2`. Verify before reading and after writing.
- v0.4 as reviewed: 772 lines, SHA-256 `0e8b0c05aaac4e1a7c2c4ffe4e1c60ce772d2b9afabee1c1f99868d19d490758` (recorded in the
  fourth review's header; not in git). v0.2 is `git show HEAD:<path>`.

## Read in full

1. The ADR v0.5. Then the four reviews — `REVIEW-adr001-v0.4-verdicts-claude-opus-5.md` (Appendix A.2 is the
   escape v0.5 answers), `REVIEW-adr001-v0.3-verdicts-codex.md` (Appendix A is the 21-probe suite),
   `REVIEW-adr001-v0.2-verdicts-claude.md`, `REVIEW-adr001-v0.1-verdicts-fable.md` — `NOTE-001-effect-inference-gap.md`,
   and the three 2026-09-20 handoffs.
2. `../033_release/ADR-001-release-scope.md` — G5, G5a, G5e (reworded twice today; check they match the ADR).
3. `tools/ext_ambient_inventory/hook_scope.py` **in full** — v0.5 specifies the registration boundary as this tool's
   reachability walk and cites `:143-150`, `:820-905` (the walk and its rejections, including `applied-local`) and
   `:913-932` (`_binding_text`). `scripts/dst/run_declared_vs_performed.sh:740-780` (the two-sided limitation idiom).
   `Makefile:500-525` (`DST_TARGETS`).
4. `packages/motoko-ext-abi/types.ail` (`Capability` `:1238-`, `ExtCtx` `:536-`, `ExtPorts` `:256-`, `ExtEntry` `:1275`),
   `src/core/ext/runtime.ail`, `src/core/journal.ail` (`:28-31`, `:628-636`, `header_of_entry` `:813-818`,
   `resume_refusal` `:1796`, the unknown-entry test `:3282-3288`), `src/core/session.ail:2607-2652`,
   `src/core/step_machine.ail:102-118`, `src/core/ports.ail` (`wake_read`, `ToolOutcome`).
5. Host side: `src/tui/src/session-journal.ts` (`JOURNAL_SCHEMA_VERSION` `:34`, `adopt` `:288`, `append` `:367`,
   `writeHeader` `:403`, `completeHeader`), `src/tui/src/session-logger.ts:404`, `src/tui/src/runtime-process.ts` (wake routing, `:1287`).
6. `../013_core_architecture_for_dst/ADR-003-session-journal-and-resume.md` (D3, D5), `ADR-004-journal-as-evaluation-source.md` (D1, D5),
   `../017_extension_handling/ADR-001-extension-abi-evolution.md`.
7. Every `packages/*/register.ail` and delegated `make_hooks`; `packages/motoko-ext-compose/compose.ail` (`:312,326,921,1018,1110`).

## What v0.5 decided, each a claim under test

| Item | v0.5 decision | Where |
|---|---|---|
| N31, N33 | Boundary = **named top-level functions only**, bound directly in the list, `DescribeTools` included; specified as `hook_scope.py`'s reachability walk; `_binding_text` fails closed; explicit new verdict; tree-wide `ext_hook_scope` in `DST_TARGETS` and G5e; residual for registry-installed extensions stated; escape recorded as D2 fact 4; escape probes join the suite | D2, D7, freeze 2, 8 |
| N32 | Count stated: twelve `let`-bound bindings in ten packages (named), 46 constructor sites; captured registration values become parameters of a named list-builder; pricing deferred to the plan | D2 |
| N34 | Native schema-2 header `schema_version: 2`; `header_of_entry` widens; two shapes named; refusal text a schema-1 runner actually sees, per shape; unknown-entry amendment withdrawn (`journal.ail:3282-3288`); un-taken second rewrite and its cost recorded | D7 |
| N35, N40 | `DecisionInvocationState` gains `allowance_millicents`, `known_spend_millicents`, `outstanding_millicents`, `run_cost_millicents`, `run_cap_millicents: Option[int]`; host-stamped; child evaluates; `run_cap = None` when unmetered or `max_cost_millicents ≤ 0`; finalize-site query bounded by the decision allowance alone | D3, D5 |
| N36 | `Identity` resume row: refuses when the invoked registry introduces an identity absent from the epoch's ledger, across profile switches; forced waiver starts at zero allowance until explicit migration | D6 |
| N37 | `Descriptor` row split: question/limit change refuses; interpretation-only change appends an epoch and continues | D6 |
| N39 | No `XFAIL` class; trigger-shape and escape rows in the script's existing `ok "LIMITATION n still holds"` / `bad "FIXED UPSTREAM"` idiom | freeze 8, D7 |

## Questions to answer explicitly

**Q-1. Attack the named-function arm.** It is now the whole boundary. Re-run the 21 + escape probes
(sources in the third and fourth reviews' appendices) and try to break a *named top-level* callback on
v0.33.0: through an imported helper, a generic, a higher-order argument, a record it receives rather
than builds, mutual recursion, a `match` on a captured sum, anything. If you cannot, say what you tried.

**Q-2. Is the walk what v0.5 says it is?** Read `hook_scope.py` and confirm or refute: that it walks
transitively into named callees; that `applied-local` at the cited line rejects `w.f(ctx)` inside a named
function; that `_binding_text` currently accepts inline lambdas of any body and follows `let`-bound lambdas;
that "registration_only" is where fact 4's construction is filed today. Is specifying the boundary as this
walk sound, or does it make an inventory tool into a load-bearing gate it was not designed to be?

**Q-3. Identity and allowances.** Does the `Identity` row plus zero-allowance-on-waiver close the minting
path in every case the fourth review listed (same-profile reorder, cross-profile replace/reorder,
reinstall), and does it interact badly with the split `Descriptor` row or the epoch fold? Is the admission
equation now complete and evaluable from `DecisionInvocationState` alone?

**Q-4. Schema shapes.** Are the two schema-2 shapes and their refusals correct against `header_of_entry`,
`adopt`/`writeHeader`/`completeHeader`, and the unknown-entry test? Is the withdrawn amendment really
unnecessary?

## Then the full pass

D1–D7, the freeze-evidence list, the D7 table, and all four response tables. The fourth review said findings
per round have not converged (13, 9, 8, 10): report only what you verified. Number new findings from **N41**.

## Output

Write `REVIEW-adr001-v0.5-verdicts-claude-fable-5-1.md` in this directory, in the structure of the fourth
review: 1 verdict and freeze readiness; 2 remaining blockers; 3 new findings N41+; 4 resolution of N31–N40
(resolved / partial / not, with what you verified against); 5 Q-1..Q-4; 6 D1–D7; 7 before-freeze /
before-plan / after; 8 method and limitations; appendix of probes; provenance with model id as you report
it, the session caveat above, HEAD, and the ADR SHA before and after.

## Rules

- Read-only against the tree except the one review file; probes in a scratch directory outside the repo,
  quoted into an appendix. No commits. Do not touch Herdr pane `w3:p1` or `/workspaces/motoko_agent-eval`.
