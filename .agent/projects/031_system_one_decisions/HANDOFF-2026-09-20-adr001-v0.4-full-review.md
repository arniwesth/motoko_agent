# HANDOFF 2026-09-20 — ADR-001 v0.4: full independent review

Requested by the operator. Drafter of v0.3 and v0.4: Claude Fable 5.1 (Herdr pane `w3:p5V`, agent
`release-review`). Third review (of v0.3): Codex `gpt-6-astra`. The reviewer of v0.4 must be neither.
This is a **full review** with the v0.3 → v0.4 delta used to weight time, not to bound scope.

## Grounding

- Branch `arniwesth/031-abi-8-0`, HEAD `2f3ee4d1`. `src/core`, `packages`, `tools`, `scripts`, Makefile
  are byte-identical to the second review's HEAD `2062605`. Extension ABI `7.4`; AILANG `v0.33.0`.
- **v0.4 is the working-tree file**, uncommitted:
  `.agent/projects/031_system_one_decisions/ADR-001-extension-owned-structured-decisions.md`,
  772 lines, SHA-256 `0e8b0c05aaac4e1a7c2c4ffe4e1c60ce772d2b9afabee1c1f99868d19d490758`. Verify before reading and after writing.
- v0.3 as reviewed by Codex: 633 lines, SHA-256 `a78d505fe1685467276b2593fde0f17b473983b4c1f9fb5741e5635e78371ba1`.
  It is not in git; the third review's header records it. v0.2 is `git show HEAD:<path>` (491 lines).

## Read in full

1. The ADR v0.4, then `REVIEW-adr001-v0.3-verdicts-codex.md` (all of it, including Appendix A: the
   21 compiler probes v0.4's D2 now rests on), `REVIEW-adr001-v0.2-verdicts-claude.md`,
   `REVIEW-adr001-v0.1-verdicts-fable.md`, `NOTE-001-effect-inference-gap.md`, both 2026-09-20 handoffs.
2. `../033_release/ADR-001-release-scope.md` — G5 and G5e, which v0.4 reworded (N30).
3. `packages/motoko-ext-abi/types.ail` (`Capability` `:1238-`, `ExtCtx` `:536-`, `ExtPorts` `:256-`, `ExtEntry` `:1275`),
   `src/core/ext/runtime.ail`, `src/core/journal.ail` (`:28-31`, `:628-636`, `resume_refusal` `:1796`),
   `src/core/session.ail` (`:2607-2652` resume reset; `:2231-2239`), `src/core/step_machine.ail:108-118`,
   `src/core/ports.ail` (`wake_read`, `ToolOutcome`), `src/core/dst_interaction.ail:96-110`, `src/core/dst_program.ail:117-133`.
4. Host side: `src/tui/src/session-journal.ts` (`append` `:367`, `adopt` `:288`, `writeHeader` `:403`),
   `src/tui/src/session-logger.ts:404`, `src/tui/src/runtime-process.ts` (`sendWakeReply` `:1287`, wake routing).
5. `../013_core_architecture_for_dst/ADR-003-session-journal-and-resume.md` (D3, D5, the one-rewrite rule, unknown entries),
   `ADR-004-journal-as-evaluation-source.md` (D1, D5), `../017_extension_handling/ADR-001-extension-abi-evolution.md`.
6. `packages/*/register.ail`, `packages/motoko-ext-compose/compose.ail` (`:312,326,921,1018,1110`),
   `packages/motoko-ext-progress-contract-guard/register.ail:20`, `tools/ext_ambient_inventory/hook_scope.py`,
   `scripts/dst/run_declared_vs_performed.sh`.

## What v0.4 decided, each a claim under test

| Item | v0.4 decision | Where |
|---|---|---|
| N23 | Context types give **restricted supplied authority**, not compile-time purity; three measured facts; **registration discipline**: named top-level function, or a lambda whose body is one call to one, everything else fails closed in `hook_scope.py`; missing-field faults are host control failures; the 21-probe suite is the regression; discipline is freeze evidence | D2, freeze 2 |
| N24 | `Trace` added to `ToolProvider`; `{FS}` kept; helpers take `PureCtx` or the smallest ports record, no dummy ports; exported constructors for every view | D2, D7 ABI row |
| N25 | Success-returning journal append; failed reservation → no dispatch; failed observation → no reply, reservation retained; failed application → run stops; never `Unavailable`; process-crash durability; duplicate/late-reply and stdin-ownership rules | D5 |
| N26 | Effective policy epoch folded from header + snapshots; `Descriptor` row compared against it, across profile switches; allowance fixed at first registration; identity includes install index; explicit migration only; case list; ADR-003 amendment | D6 |
| N27 | Appended `schema_promotion` entry as the fold boundary; schema-1 readers refuse at first unknown entry; upgraded-session fixture resumed twice; ADR-003 amendment | D7 |
| N28 | Two allowances: persistent decision allowance per session identity; per-run cost cap (ADR-003 reset); admission equation; no refund, no re-charge; unmetered case | D5 |
| N29 | Host adapter sole float normalizer; child validates integers only; one versioned fixture-vector set; SDK retries disabled; end-to-end deadline; contract version tied to normalization | D3 |
| N30 | `XFAIL` class in `run_declared_vs_performed.sh`; target green with expected-limitation rows listed; discipline gate before freeze; release G5e reworded | freeze 8, 033 G5e |

## Questions to answer explicitly

**Q-1 (N23).** Is "named top-level function, or a lambda whose body is exactly one call to one" the
right discipline, and is it *sufficient* on v0.33.0? Re-run the 21 probes (sources in the third
review's Appendix A) and add any construction you think escapes the rule — in particular a named
function that itself builds a callback through a local record, and a pass-through lambda whose
single call passes a *captured* value as well as its parameters (the compose shape captures
`composition_mode`). Is `hook_scope.py` the right place, and can it fail closed on what it cannot resolve?

**Q-2 (N26, N28).** Are the policy-epoch rule and the two-allowance rule consistent with each other
and with ADR-003 D5's accepted profile switch and reset totals? Is the admission equation complete?
Does anything in v0.4 still allow a refund of an outstanding reservation or a fresh allowance on resume?

**Q-3 (N27).** Is the appended `schema_promotion` entry sound against ADR-003's one-rewrite rule and
the host's `adopt`/`writeHeader` behavior, and does "schema-1 fold refuses at the first unknown entry"
hold for the current fold, or is that itself the amendment?

**Q-4 (N30).** Does the `XFAIL` semantics resolve the contradiction without weakening the gate, and does
the reworded G5e in the release scope match freeze item 8 exactly?

## Then the full pass

D1–D7, the freeze-evidence list (items 2, 5, 7, 8 rewritten), the D7 table (new "Other records" row),
and all three response tables. Number new findings from **N31**.

## Output

Write `REVIEW-adr001-v0.4-verdicts-<your-model>.md` in this directory, in the structure of the third
review: 1 verdict and freeze readiness; 2 remaining blockers; 3 new findings N31+; 4 resolution table
for N23–N30 (resolved / partial / not, with what you verified against); 5 Q-1..Q-4; 6 D1–D7; 7
before-freeze / before-plan / after; 8 method and limitations. Provenance: model id as you report it,
HEAD, the ADR SHA before and after.

## Rules

- Read-only against the tree except the one review file; probes in a scratch directory outside the
  repo, quoted into an appendix. No commits. Do not touch Herdr pane `w3:p1` or `/workspaces/motoko_agent-eval`.
