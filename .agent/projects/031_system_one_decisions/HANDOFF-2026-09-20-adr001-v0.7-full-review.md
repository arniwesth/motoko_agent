# HANDOFF 2026-09-20 — ADR-001 v0.7: full independent review (Claude Fable 5.1, fresh session)

Requested by the operator. Drafter of v0.3–v0.7: Claude Fable 5.1 (Herdr pane `w3:p5V`, agent
`release-review`). You are a **fresh session of the same model**; you have none of the drafter's context and
must not ask for it. Independence here is of session and of evidence, not of model — say so in your
provenance, as the fifth review did. Prior reviews: v0.3 Codex, v0.4 Claude Opus 5, v0.5 a fresh Fable
session, v0.6 Codex. This is a **full review** with the v0.6 → v0.7 delta used to weight time, not to bound scope.

## Grounding

- Branch `arniwesth/031-abi-8-0`, HEAD `2f3ee4d1`. `src/core`, `packages`, `tools`, `scripts`, Makefile are
  byte-identical to the second review's HEAD `2062605`. Extension ABI `7.4`; AILANG `v0.33.0`.
- **v0.7 is the working-tree file**, uncommitted:
  `.agent/projects/031_system_one_decisions/ADR-001-extension-owned-structured-decisions.md`,
  1107 lines, SHA-256 `45378ccd835f1aedaed38f4ba56aa62b02a691af0763b8da79ccc821378abd06`. Verify before reading and after writing.
- v0.6 as reviewed: 990 lines, SHA-256 `f24b2b812ed3e8541fc3a2583c377c1a7e0b55efe4f3c0864a74e9369f01d282` (in the sixth
  review's header; not in git). v0.2 is `git show HEAD:<path>`.

## Read in full

1. The ADR v0.7. Then the six reviews, newest first: `REVIEW-adr001-v0.6-verdicts-codex.md` (its Q-1 ledger of all 45
   registration sites and its A.3 escapes are what v0.7 answers), `REVIEW-adr001-v0.5-verdicts-claude-fable-5-1.md`,
   `REVIEW-adr001-v0.4-verdicts-claude-opus-5.md`, `REVIEW-adr001-v0.3-verdicts-codex.md`, `REVIEW-adr001-v0.2-verdicts-claude.md`,
   `REVIEW-adr001-v0.1-verdicts-fable.md`; `NOTE-001-effect-inference-gap.md`; the five 2026-09-20 handoffs.
2. `../033_release/ADR-001-release-scope.md` — G5, G5a, G5e.
3. `tools/ext_ambient_inventory/hook_scope.py` **in full** — v0.7 specifies changes to `Scope.locate` (`:632-658`),
   `_binding_text` (`:910-942`), `verdict` (`:953-956`), `emit_hook_scope` (`:1122`) and the `walk`/`locate` split
   (`:1044-1046`); `tools/ext_ambient_inventory/derive.py` (`installable_extensions`, `:888`); `scripts/dst/run_declared_vs_performed.sh`
   (`:751-771`, `:945-1000`); `Makefile` (`DST_TARGETS` `:507-519`, `ext_hook_scope` `:3214`).
4. `src/core/tool_catalog.ail:100-115` (the `DescribeTools` invocation v0.7 changes), `packages/motoko-ext-abi/types.ail`
   (`Capability` `:1238-`, `ExtCtx` `:536-`, `ExtPorts` `:256-`, `ExtEntry` `:1275`), `src/core/ext/runtime.ail`
   (`ext_set_digest` `:1107-1122`, `:343-347`), `src/core/ext/registry_normalize.ail`, `src/core/journal.ail` (`:28-31`, `:628-636`,
   `:813-818`, `:1796-1806`, `:3282-3288`), `src/core/session.ail:2607-2652`, `src/core/step_machine.ail:102-118`.
5. Host side: `src/tui/src/session-journal.ts` (`:34`, `:288`, `:367`, `:403`), `src/tui/src/session-logger.ts:404`,
   `src/tui/src/runtime-process.ts` (wake routing, `:1287`).
6. `../013_core_architecture_for_dst/ADR-003-session-journal-and-resume.md` (D3, D5), `ADR-004-journal-as-evaluation-source.md` (D1, D5),
   `../017_extension_handling/ADR-001-extension-abi-evolution.md`.
7. The four `DescribeTools` sites: `packages/motoko-ext-a2a/a2a.ail:167`, `packages/motoko-ext-agentcli/agentcli.ail:208`,
   `packages/motoko-ext-ailang-docs/ailang_docs.ail:49`, `packages/motoko-ext-herdr/herdr.ail:2023-2032`; and every
   `register.ail` / delegated `make_hooks` for the delegation-resolution question.

## What v0.7 decided, each a claim under test

| Item | v0.7 decision | Where |
|---|---|---|
| N49 | `DescribeTools((Json) -> [ToolSchema])`; the catalog passes the entry's `config`; fixed catalogs ignore it | D2, D7 ABI row, freeze 2(c) |
| N50 | Claim narrowed: the host hashes registration-time state returned through the channel and a named callback can read no *registration-time* state that was not; pure module constants/imports, transcript, artifacts, evidence named with their own provenance; the `question_config` obligation retained; a violation is a conformance failure that strict query replay detects and the live `Descriptor` row does not; restricted question-only view for `prepare` recorded as not taken; cost stated as an expectation the plan measures (reference per epoch, hash once, bounded size, migration measurements) | D2 |
| N51 | Fact 6; the shape rule covers the whole registration: closed grammar (`{ config, caps }`, literal list or delegated call), resolution lexical at every hop (locals and parameters before declarations), `Scope.locate` changes, unresolvable delegation fails closed | D2 |
| N52 | Gate = **total registration-shape result** per installable extension (supported return shape, every payload enumerated, every payload named/unshadowed, every delegation resolved), distinct from walk residue; computed/unknown lists fail; `emit_hook_scope` exits nonzero; fixture list stated | D2, freeze 8, 033 G5e |
| N53 | Forced waiver appends a snapshot with an **orphan acknowledgment** per orphaned identity (liabilities retained, predicate retired); removal with no replacement is acknowledgment alone; reappearance resumes the retained ledger | D6, D7 |
| N54 | Waived identity registered **inactive** for money and interventions, skipped like an exhausted atom; grant sets money and intervention limit separately and activates; migration moves the intervention balance; orphaning is over retained state, not syntax | D5, D6, D7 |
| N55 | Run cap applies at admission to every query, terminal included; the step machine's check is a second observation point | D5 |
| N56 | Fifth-review set scored as 20 attacks / 1 control / 3 recognizer escapes; new cases are new rows | D2, freeze 2(b) |

## Questions to answer explicitly

**Q-1. Delegation resolution (N51, N52).** Is "lexical at every hop, locals and parameters before declarations, closed
grammar, computed lists fail closed" sufficient? Re-run the sixth review's `delegate_shadow`, `computed_list` and
`registration_record` (sources in its A.3; support modules in its A.1) and try to construct a registration the bare
compiler accepts that the rule as written would classify as a resolved, named, unshadowed payload — through an
import that shadows, a delegated call into another module, a parameter that names a function, a `match` that
selects between lists, anything. Can `Scope.locate` as it stands be changed to keep scope provenance per hop, and
is the total registration-shape result implementable without changing the shipped closure-inventory verdict?

**Q-2. The waiver transition (N53, N54).** Walk every pinned case in D6 with the acknowledgment and inactive rules.
Does the orphan predicate now retire correctly on the second ordinary resume, and does anything still let a
waived or migrated identity cast a free `Immediate` vote or obtain a fresh allowance? Are the three entries
(acknowledgment, migration, grant) sufficient and non-duplicating — a second migration, a grant to an active
identity, a grant to an acknowledged orphan, reappearance after acknowledgment?

**Q-3. The narrowed channel claim (N49, N50).** Is the claim now exactly true? Is passing `config` to `DescribeTools`
the right shape against `tool_catalog.ail` and the four sites, and does anything else in `src/core` invoke it?
Is "a violation is a conformance failure that strict query replay detects" actually true for every path from
general configuration to a prepared request?

**Q-4. The gate criterion (N52).** Against the verdicts `derive_hook_scope` actually emits and the eighteen
installable extensions at HEAD, is the total registration-shape result well-defined, and does the fixture list in
D2 make it go red on both shadows and a computed list and green on a migrated tree?

## Then the full pass

D1–D7, the freeze-evidence list, the D7 table, and all six response tables. Findings per round so far: 13, 9, 8,
10, 8, 8. Report only what you verified. Number new findings from **N57**.

## Output

Write `REVIEW-adr001-v0.7-verdicts-claude-fable-5-1.md` in this directory, in the structure of the fifth review:
1 verdict and freeze readiness; 2 remaining blockers; 3 new findings N57+; 4 resolution of N49–N56 (resolved /
partial / not, with what you verified against); 5 Q-1..Q-4; 6 D1–D7; 7 before-freeze / before-plan / after; 8
method and limitations; appendix of probes; provenance with model id as you report it, the session caveat, HEAD,
and the ADR SHA before and after.

## Rules

- Read-only against the tree except the one review file; probes in a scratch directory outside the repo, quoted
  into an appendix. No commits. Do not touch Herdr pane `w3:p1` or `/workspaces/motoko_agent-eval`.
