# P1.1 evidence — core onto ABI 8.0 (PLAN-001 (031), line R)

HEAD before: `a6f03242`. AILANG v0.33.0 (the pin). Delegate run 2026-09-21.

## What is here

| file | what |
|---|---|
| `p11_core_check.sh` | `check` / `test` any `src/core` module against THIS checkout's ABI 8.0 in a throwaway workspace. Its header says why the tree cannot be checked in place until P1.2 and why the workspace is immune to the `ailang.lock` trap |
| `CHECKS.log` | exit checks 1 and 2: `ailang check` on all 12 touched modules, `ailang test` on the 4 with tests |
| `p11_mutgate_spec.tsv`, `p11_mutgate.sh`, `mutgate-result.tsv` | exit check 5: 26 rows, run by `p11_mutgate.sh` in a throwaway copy (or `--clone-from` for intake) |
| `inventory.py`, `INVENTORY.tsv` | the inventory deliverable; the generator states every derivation |
| `ctor_collision/` | the constructor-collision probe (below): six consumers, `run.sh`, `RESULT.txt` |

## Exit checks

1. **`ailang check` clean on every module touched** — 12/12 (`CHECKS.log`). In place, every one of
   them fails inside `pkg/sunholo/motoko_ext_test_dummy/register` (a 7.4 package) before reaching the
   module: `ext/runtime.ail` imports `ext/registry_generated.ail`, which imports all 18 packages, and
   `tool_phase.ail` (hence `session.ail`) imports `motoko_ext_scratchpad/ws_loopback`. The workspace
   replaces exactly those two with stand-ins — a registry GENERATED by the real P0.5 template over one
   synthetic 8.0 extension, and a signature-identical `exec_scratchpad_cell_ws` — and nothing else.
2. **The ext fixtures green** — `p11_core_check.sh test src/core/ext/runtime.ail` 24/24,
   `src/core/ext/registry_normalize.ail` 17/17, `src/core/tool_catalog.ail` 5/5;
   `src/core/ext/exit_manifest.ail` (untouched) 2 passed, 0 failed, 2 skipped — its two contract
   properties have no generator and `ailang test` exits 1 for that without `--allow-skips`, a
   pre-existing condition (`P11_ALLOW_SKIPS=1`).
3. **Line-neutral** — `make anchors` exit 0, all 10 anchors in place; `make driver_leaf_inventory`
   exit 0 with output byte-identical before and after (diffed). `session.ail` has the same line count;
   every edit there sits on an existing line.
4. **Precedence** — `merge_tool_policy_precedence_test` (Deny > Pending > Allow > NoOpinion) and
   `merge_finalize_precedence_test` (ContinueWithFeedback > Accept > NoDecision, `Accept` nullary) in
   `runtime.ail`, plus the existing `merge_*` and `dispatch_smoke_record_hooks`; mutgate rows
   `precedence_tool_policy` / `precedence_finalize`.
5. **mutgate** — `p11_mutgate.sh` from a throwaway copy of the working tree, **26/26 discriminate**, every row GREEN → RED → GREEN with bytes identical (`mutgate-result.tsv`; exit 0). The brief's four named rows: `vote_family_finalize` +
   `vote_family_tool_policy`; `ext_set_digest_kind_only`; `config_digest_{tool_names,exit_label,
   exit_enabled,work_label}`; `describe_tools_config`. Then nine `stamp_*` rows (one per dispatcher:
   the fold's projection set to an empty stamp → that fold's test red), the bootstrap rule, the two
   precedence rows, and the digest/family rows.

Also: `make registry_gen_check` exit 0 (the committed `registry_generated.ail` is the P0.5 template's
output — the template was verified, not redone).

## What moved, and what did not

- **Views projected per entry, in the folds.** Every dispatcher still takes the host's full `ExtCtx`
  (built once per turn in `session.mk_v2_ext_ctx`); `runtime.entry_base` = `pure_ctx_of(ctx, e.config)`
  once per entry per dispatch, and the world-threading folds wrap it per atom in the row's view
  (`ai_ctx`, `provider_ctx`, `intercept_ctx`, `fs_ctx`, `process_ctx`) around the holder-stamped world —
  the same site as the 7.4 `{ ctx | world: stamp_holder(world, id) }` re-seat. No callback receives
  `ExtCtx`; no helper takes `ExtPorts` on a callback's behalf.
- **`ext_config` stamped** from `ExtEntry.config` on every view; nine tests observe it from inside each
  callback with two differently-configured entries.
- **`normalize_registration(id, config, caps)`**; `config` carried unchanged; **vote families**
  (`vote_family`): a legacy and a new variant in one family is `SecondVote`, one finalize + one
  tool-policy atom coexist.
- **Config digest** (`ext_config_digest`, `registry_normalize.ail`): sha256 over the CANONICAL JSON
  (keys sorted by byte value at every depth) of `{ config, atoms: [per-atom data positions] }` —
  `ToolProvider` names, `ExitIntent` label + `enabled`, `WorkInFlight` label, in list order. The
  decision descriptor is NOT in it (D6's `Descriptor` row owns it; N58 enumerates the three). `encode`
  preserves key order, so canonicalisation is not optional. **`ext_set_digest` stays kind-only**
  (test + mutation) and gains the two decision kinds (a `SolverJudge` → `DecisionSolverJudge` change
  moves it, per D7).
- **`DescribeTools` receives `e.config`** (`tool_catalog.ail`); the configured-vs-empty catalog test.
- **`ToolProvider`'s row gains `Trace`**: `dispatch_tool_handle` and its chain, and the two callers
  `tool_envelope_dispatch.dispatch_tool_envelope` and `dst_hook_guard.guarded_dispatch_tool_handle`.
  `packages/motoko_scratchpad/ws_loopback.ail` calls the former from a row without `Trace` — P1.2's.
- **`Accept` nullary**: `session.classify_candidate` finalizes with `candidate`.
- **`session.ail`, re-signature only**: the two `ExtCtx` literals gain `verification: NotReached`,
  `tool_evidence: empty_tool_evidence()`, `decision_state: None` (P1.4r owns their fixtures and the
  decision-site `Some`); `ToolFailed` imported by name (below). `tool_phase.ail` needed no edit.
- **Fixtures**: `FixtureOverrides` on the views with a `config` field; `stub_step.deny_all_rt` re-typed.

## Finding: constructor-name collision on the pin (`ctor_collision/`)

`session.ail` failed to check with: `match arm constructor 'ToolFailed' belongs to ADT
'ObservedToolOutcome', not 'ToolOutcome'`. The core's `ports.ToolOutcome` has had `ToolFailed` since
D3 of 013; ABI 8.0's `ObservedToolOutcome` (031 D3) declares another. The probe (`run.sh`, six
two-line consumers) measures v0.33.0's rule:

| consumer | imports | result |
|---|---|---|
| `no_abi` | ports only | clean |
| `ports_first` | ports, then the ABI (unrelated name) | clean |
| `abi_first` | the ABI (unrelated name), then ports | **fails** — the ABI's `ToolFailed` wins |
| `abi_first_named` | as above, `ToolFailed` imported BY NAME from ports | clean |
| `both`, `both_named` | one module using both constructors | **fails** either way |

So: an imported module's constructors enter the importer's table even under a selective import list;
the EARLIEST import wins; a named import binds. `tool_phase.ail` was already clean only because it
imports `ToolFailed` by name. The fix applied here is the same: `session.ail` names `ToolFailed` in its
`ports` import (line-neutral). **What this leaves for the orchestrator:** a module that must build
`ObservedToolOutcome` values FROM `ports.ToolOutcome` values — the P1.4x evidence construction, line X —
cannot name both on this pin (`both_named`). That is Amendment 1's class of defect across modules,
and an ABI rename (`ObservedToolOutcome`'s `ToolFailed`; the sweep found `ToolPending` and `Disabled`
also declared in core, `phase_vocab.ail:1293` and `context_limit.ail:44`, both imported by name where
used) would be a numbered amendment, not this part's. Nothing in R needs it.

## Inventory (`INVENTORY.tsv`)

156 tracked files outside `packages/` and `.agent/` name `ExtCtx`, `ExtEntry` or `register_with_config`.
See the envelope for the per-owner counts. Every `UNOWNED` code row is a finding; the two largest
groups: `scripts/dst` and `scripts/verify_*` / `smoke_*` (18 `ExtCtx` literals, 44 `ExtEntry` literals,
2-argument `normalize_registration` in `registry_multiplicity_dst.ail`, `FixtureOverrides` literals
without `config`, callbacks typed `ExtCtx`) — `make check_core`'s `verify_*` targets and most of
`DST_TARGETS` run them; and the 21 `tools/ext_ambient_inventory` / `ext_call_inventory` self-test
fixtures (textual gate inputs; the 7.4 context name is probe text). Not migrated here, as the brief
requires.

Counts re-derived at HEAD (`inventory.py`, regex `(:|->)\s*ExtCtx\s*(=\s*)?\{` for a context
literal; `{ id: …, caps:` for an entry literal): **ExtCtx literals 29 in 25 files tree-wide, 21 in 20
outside `packages/`, 3 in 2 in `src/core`** (plan said 37 in 28); **ExtEntry literals 11 in 3
`src/core` files** (plan said 8 — it missed `smoke_hook_b`, `chain_base_hook`, `pending_hook`), 44 in
13 files outside `packages/` and `.agent/`.
