# REVIEW: PLAN-004 v1.2 delta verdicts (Codex)

Date: 2026-09-15 UTC. HEAD `39208145b7b039500b6639b7e70e476e91bab288`, branch `arniwesth/013-plan003-and-herdr` (`git rev-parse HEAD`; `git branch --show-current`). Subject: proposed PLAN-004 v1.2 and its operator graph. Inputs: `BRIEF-plan004-v1.2-review.md`, `REVIEW-plan004-v1.1-delta-verdicts-codex.md`, PLAN-004 v1.2 (P), `.dagr/run-plan004.json` (G), ADR-004 D1/D3/D8 (A), herdr `dagr.ail`/`herdr.ail`, the installed AILANG source and repo's `ailang/` checkout. Method: read-only source/graph inspection, strict dagr checks and snapshots, a separate synthetic transaction and additivity audit. I did not edit the plan or graph; commit; operate a herdr pane or agent; or run DST, replay, prototype, profiling or other heavy execution. Evidence: `/tmp/claude-1001/-workspaces-motoko-agent/d8b1d53b-1a2d-4c49-b4ec-fc412754e86a/scratchpad/codex-review-plan004/v1.2/`. This review contains only public counts, digests, indices, identities and contract names. `P:<line>` means `.agent/projects/013_core_architecture_for_dst/PLAN-004-implement-adr-004.md` at HEAD; `G:<id>` means that task in the captured graph `graph_observed.json` (SHA-256 `8b10e8be766023376818c8c83fcf9d5b3f2e5eda58acdfa65e5de0ab1154945e`). The live G moved from `REV2 done`/one attempt to `REV2 working`/two attempts during this read (`python3` inventories before/after; final graph and captured SHA-256 equal); audit conclusions use the captured two-attempt graph.

**Overall verdict: ACCEPT WITH CORRECTIONS.** The core review transaction is append-only and the author-plus-review gate stays waiting on three RETURNs in an independent reproduction; it becomes ready on an accepting author settlement (`transaction_checks.txt`: all ten stages exit 0, `[]`, gate `waits A` through stage 07 and `ready` at 08; §2). Strict graph lint also passes (`dagr check .dagr/run-plan004.json --strict --json` → exit 0, `[]`). The reviewer launch cue, the first two-file receipt, the matrix and several downstream provenance claims require the exact edits in “Verdict for PLAN1G.” None licenses a real entry or protected pin before the accepted-v5/PLAN2G gates (`P:15–29,281–300`). This is the third plan round: a RETURN would require an operator directive (`P:3–8,145–153`); the listed corrections can be applied and named before `PLAN1G` settles (`P:220–223`).

## §1 — Five required changes from v1.1

| v1.1 requirement (`REVIEW-plan004-v1.1-delta-verdicts-codex.md:105–109`) | Disposition | Evidence and remaining correction |
|---|---|---|
| 1. Append-only review/RETURN loop, real author and implicated P1 parts | **PARTIAL** | P:124–157 and G:`PLANW,V5,PSYNC,P1.1` put policy on authors; G:`PLAN1G,V5G,PLAN2G,P1.1G` join author and review; `PLANW·a3` has no a4 forecast (G:`PLANW`). Independent §2 sequence works. Reviewers queued with an author dep display `waits author` while the author is in `review`; P1R additionally needs a separate aggregate-return rule because its parts must be `done` to unblock downstream implementation. The third-round `·ask` future remains stale after an unblock unless removed (`P:133–157,592–605`; §2). |
| 2. Two files, receipts and a fresh-session rule | **ADDRESSED** | P:780–808 distinguishes operator plan from producer observation and requires marker/linkage/run-file receipts; P:286–300 carries history only in the operator v2 file; `dagr.ail:215–246,256–284` confirms that interpretation. The first delegation needs a provisional receipt because its producer run file does not yet exist; fix the duplicate `producer=` label (§3). |
| 3. D8 literal cases and result artifact | **PARTIAL** | P:331–383 adds M15, a V5 wording decision (P:266–271,732), independent digest expectations and `MATRIX.tsv`. Several M15 cases cannot reach the named later check, and one `position` column cannot compare pinned versus observed position (§4). |
| 4. Import/checker and independently checked provenance | **PARTIAL** | P:414–451 adds full import statements, C-side symbol checks and multiline type handling; P:476–495 and 553–575 add identity/cache/loader tables. The overlapping import-region rule, trailing `deriving`, same extractor called twice, mismatched scan-root kinds, incomplete cache record and unresolved absolute lock path remain (§5). |
| 5. P3 ordering, escalation and estimates | **PARTIAL** | P:675–684 puts the provisional tolerance before P3.3 and one ≤150-call dev/exposed escalation inside P3.2; P:181,771–774 fixes the base 18½–22 and 30–37½ sums. Escalation beyond P2.2's ≤100-call calibration needs a new measured tolerance/peak and adds up to one day to the maximum (§6–§8). |

## §2 — Independent §0.10 transaction

I built `build_transaction.py` and `00a_author_review_reviewer_queued.json` through `08_accept_gate_ready.json` under the evidence directory, using only the dagr v3 contract (`dagr --skill`: attempt projection lines 79–83,95–111; causes 103–105; send-back 250–265; policy materialization 338–355). `transaction_checks.txt` records `dagr check <stage> --strict --json` exit 0, `[]` and stripped `dagr view --snapshot` rows at **each** step:

| Stage | Author / review / gate observed | Evidence |
|---|---|---|
| `00a`, `00` | Author `review` with a1 `working`; reviewer queued `waits A` at `00a`, then working by a manual start at `00`; gate `waits A`; author policy shows pass, `⟲ A·a2`, `≈ G·ask`. | `transaction_checks.txt:00a,00`; G review tasks' author deps; P:130–134. |
| `01` RETURN 1 | Review a1 `done` with RETURN receipt; author a1 `rejected`, a2 `working` with `sent_back` ref `R·a1`; policy moved to `A·a3`; gate `waits A`. | `transaction_checks.txt:01`; `01_return1.json`; P:135–144. |
| `02` re-review / `03` RETURN 2 | Author a2 `review` and review a2 `working` with `followup` ref `A·a2`; second RETURN settles both, opens a3 and removes the a3 fail future, so **no a4 is forecast**; gate waits. | `transaction_checks.txt:02,03`; P:133–148. |
| `04` third review / `05` third RETURN | Review a3 follows author a3; third RETURN leaves latest author attempt `rejected`, task `blocked` with `unblock`, policy only pass/`G·ask`, no a4; gate `waits A`. The ask renders `if ✗✗✗`. | `transaction_checks.txt:04,05`; `05_return3_blocked.json`; P:145–148. |
| `06` directive / `07` re-review / `08` ACCEPT | A `directive` event (`verb: unblock`) precedes a4 `sent_back` ref `R·a3`; review a4 `followup` ref `A·a4`; ACCEPT settles both latest attempts `done`, and only then G becomes `ready`. | `transaction_checks.txt:06–08`; P:138–153. |

The script compares every prior `events` prefix and every previously settled attempt byte-for-byte between successive documents; it prints `append-only prior settled attempts and event prefixes: PASS` (`build_transaction.py` final audit; `transaction_checks.txt` last line). `cause.ref` always names an earlier attempt and all ten strict checks return `[]`; moving `A·a2`→`A·a3` and consuming `A·a3` avoids `E164` (`transaction_checks.txt:01,03`; `dagr --skill` lines 338–355). Author policies render in `review` with a live working attempt, in `working`, and in `blocked`; a queued reviewer with `deps:[A]` still renders `waits A` at `00a`, although opening it manually at `00` is strict-valid (`transaction_checks.txt:00a,00`; G:`V5R,P1.1R,PREV,P1R` deps). Clarify that manual review launch is intentional when the author artifact is in `review`, or give reviewers a separate submission dependency; following only the displayed ready queue otherwise stalls the early V5/P1.1 reviews (`P:130–134,277–299,325–329`; G reviewer deps).

The **no-fourth-round rule is operator protocol, not a dagr invariant**: `06_unapproved_a4.json` deletes the directive from the valid a4 document and still gives `dagr check --strict --json` exit 0, `[]`. The writer/gate must require the unblock directive and its receipt (`P:145–153,792–801`). On a real fourth-round unblock, remove the already-consumed `G·ask` future and record the new authorized round limit/current intent in the same mutation; leaving it in `06_directive.json` renders another `if ✗✗✗` forecast for attention already answered (`transaction_checks.txt:06`; `dagr --skill` lines 338–355). G:`PLANW` correctly has no `PLANW·a4` forecast while a3 is under review (Python policy inventory; P:217–223).

The P1R aggregate path is a distinct case. If every P1 part stays `review`/open until P1R accepts, G:`P1.5,P1.9b,P1R` deps cannot become ready: P1.5 waits earlier parts, P1R waits P1.9b, and P:602 says parts are `done` by an accepting P1R verdict (`P:124–142,390–403,592–605`; Python dep inventory). P1 implementation parts must instead settle `done` on their own implementation receipts; P1R then reviews that completed tree. On a P1R RETURN, preserve each implicated prior `done` attempt and append a new `sent_back` correction attempt, then require latest P1R acceptance and latest part `done` before P1G; do **not** apply P:141's settle-open-attempt-`rejected` rule to a previously done part. Independent `09_aggregate_return_preserve_done.json` keeps A·a4 `done`, appends review R·a5 `done` RETURN and A·a5 `working` `sent_back` ref R·a5; `dagr check --strict --json` → exit 0, `[]`, `dagr view --snapshot` → gate `waits A` (scratch command output). P:156 hints at this exception but does not state the necessary initial part settlement and no-rewrite rule.

G:`PLANW`'s retro a1/a2 are **contract-valid reported history**, not producer-observed proof: they say `note: recorded retroactively`, have `evidence: reported`, and `PLANW·a2` refers backward to `REV1·a3`; a3 refers to `REV2·a1` (Python attempts inventory; G:`PLANW,REV1,REV2`). Recorded order is a1 work 18:04–18:12, REV1 a3 18:22–18:33, a2 work 18:34–18:46, REV2 a1 18:49–19:00, a3 start 19:10, REV2 a2 start 19:14 (`python3` timestamps inventory). No attempt_started/settled events exist for retro a1/a2, but the 19:13 `note` records their addition (Python event inventory; G events); their exact historical start/end times and actor remain operator reports. The review receipt is `reported`, so do not call those attempts verified (`dagr --skill` evidence tiers 217–246; G:`PLANW`).

## §3 — §8 two files and authorities

The table at P:787–790 matches A2: `dagr.ail:215–246` seeds state/deps/owner/criteria/note/project, creates one `operator`/`reported` attempt for terminal tasks and **no** attempts/events/policy for other tasks; `seed_tasks:256–266` skips existing ids; `seed_from_plan:269–284` silently seeds nothing if unreadable. Thus the operator graph can carry v1.2 history into v2, while the producer run file independently carries observed delegate panes/liveness/outcomes (`P:286–300,780–795`; `dagr.ail:215–284`). Marker contents can change by a second valid declaration in one session, so a fresh-session receipt is an operator rule; `herdr.ail:585–599` and `dagr.ail:395–397` support P:802–808's stated marker/linkage/run-id check. Strict-clean operator G is not a producer uptake receipt (`dagr.ail:269–284`; P:797–808).

The note receipt at P:796–801 is administratively useful but not mechanically enforced by `dagr check`: G `note` events accept a `detail` string, and strict lint validates structure, not whether a commit exists or a producer attempt is lost (G event inventory; `dagr check .dagr/run-plan004.json --strict --json` → `[]`; P:796–801). Its template says `producer=` **twice**; use `producer_run=<path> producer_state=<state>/<attempt> plan_state=<state>/<attempt> task=<id> evidence=<digest-or-commit>` (`P:796–798`). Before the **first** Delegate there is no seeded producer run file to compare, so write a provisional `producer=absent` receipt, require the first reply's successful linkage, then append the actual two-file receipt before another delegation or gate (`dagr.ail:272–284`; P:796–808,815–821). A `done` operator settlement beside a producer `lost/failed` attempt or nonexistent commit needs a documented ruling, not just a note; P:799–801 assigns that to the operator, which is enforceable as a gate criterion/receipt rather than by lint. The PLAN2G marker, first-reply and v2-id checks are the right fresh-session receipts (`P:297–300,802–808`; G:`PLAN2G`).

## §4 — M15 and MATRIX.tsv

D8 P1 literally asks for one known-divergent and one known-refused case per refusal family (A:920–930); V5's express confirm-or-revise question is the right **decision boundary**, because a preprocessing refusal cannot always have a replaying counterpart (`P:266–271,362–366,732`). M15 is a provisional map, not proof: no evaluator/fixtures or `MATRIX.tsv` exist at HEAD (`P:72–76,333–342`; G P1 parts queued), and each near miss must first show it passes its own family's refusal before the named later check.

| M15 rows (P:368–383) | Reachability / first-check assessment, using D1/D3 at A:390–409,451–502,665–733 |
|---|---|
| `ChainBreak`, `UnexpectedToolResult`, `MalformedToolArguments` | A consistently re-chained or validly attributed/decoded change can reach A3 only when there is a **next evaluated continuation call** whose request digest changes; A3 compares requests to the independent live excerpt (A:683–686). A terminal call has no “next call.” Pin that fixture and verify A1/A2/A4 are earlier-pass before claiming `AdmissionCheck(A3)` (`P:370,373–374`; A:681–688). |
| `DuplicateOrAmbiguousCallId` | Unique ids with swapped result order may still trigger D1's in-order association/refusal or seam `ToolCorrelationMismatch` **before** A2. Show the selected run, source/result order and first result; do not prescribe A2 until the mismatch survives attribution and the run appends (`P:372`; A:403–409,494–502,683–688). |
| `Association` | An excerpt “from a different run” fails D1's run/banner association even with equal counts; it does **not** pass its own refusal to A3. Replace with a correctly associated same-run excerpt differing in a request digest, or rule inapplicable (`P:375`; A:403–409,683–686). |
| Malformed/unknown entry or schema; continuation/empty; park/wake | Inapplicability is sound for `ContinuationStart`/`EmptySegment` when no call exists and for an unknown schema that cannot decode, but “every decodable near miss is a cutoff” is too broad: a known valid entry with semantically changed content can pass decoding and fail A1/A2/A3. Rule each family separately; park/wake needs v5's corrected boundary reason (P:120,371,376; A:478–502). |
| Scan and resource/identity refusals | A 15-character body is admitted/reported, not divergent; the scan changes no replay (P:377; A:856–876). `PreflightMismatch`/`GuardTripped` cannot run K1–K7, so inapplicable is defensible (P:383; A:709–733). V5 must say whether these count in D8's per-family sentence. |
| Candidate path/intent, evaluator/protected near misses | A resource-only candidate may contain a bug that changes raw model/tool inputs despite reviewed intent, so a permitted-path near miss can reach K2 if its exact changed path is outside D3's refusal list (A:665–676,713–720). P:378–381 names no concrete permissible path/fixture; a “just outside evaluator” edit can still be an evaluator or tool-implementation refusal, and an outside-protected edit that changes recorder input exposes a missing D5 closure. Pin path, intent review, protected C-side result, K0 pass and first K finding before counting it. |
| `ProgramUndecodable` | Flipping an entry program outcome and recomputing its digest changes the corpus/program identity; K0 compares corpus hashes and will refuse before K1/K2. A candidate cannot own the pinned program. Use a valid pinned program and a candidate run that emits a different outcome for a reachable K2 case, or rule this refusal inapplicable (`P:382`; A:709–733,805–818). |

`MATRIX.tsv`'s promised `expected_verdict/observed_verdict/first_finding/position` row is useful but cannot compare a **pinned** position to an **observed** position as P:336–337 and P:594–595 require: there is only one `position` and one `first_finding` field (`P:333–337`). Add `expected_first_finding`, `observed_first_finding`, `expected_position`, `observed_position`; freeze expected values in a fixture/manifest independent of the test runner, and have `make eval_matrix` fill only observed fields. The Python `hashlib` fixture generator is a separate digest implementation **when built**, but at HEAD it is a future `P1` artifact; real-entry A1 uses the live snapshot's recorded `digest_after` against a separate evaluator fold (`P:339–342`; A:681–684). M15's admission `AdmissionCheck(Ak)` is a refusal rather than the candidate's `Diverged` constructor (A:725–733; P:362–366); V5 should explicitly rule whether source divergence counts for D8.

## §5 — Provenance and source contracts

**P1.4b.** Full multiline statements/aliases are needed: `src/core/test/stub_step.ail:28–44` has continuations and three imports on line 44; `src/core/ports.ail:60–67,77–81` has multiline lists; `src/core/ports.ail:12–14` and `src/core/fs_node.ail:69–70` have `as` aliases. P:420–425 covers these forms in principle, but its examples cite `stub_step.ail:39–41` for several same-line statements (actual line 44) and `ports.ail:60–70` for a statement ending at 67. More seriously, each import statement span lies **inside** its `import-region` line span, while P:430 says any two overlapping spans are a hard error; every protected file with imports would fail generation. Define containment as an intentional exception (or hash only one aggregate region) and error only on unintended overlaps (`P:420–430`). Braced `type` spans stopping at `}` miss trailing `deriving (Eq)` (`src/core/types.ail:81–86`; P:426–432); include the deriving clause in the protected declaration. A non-braced union really can run over several lines and a deriving clause (`src/core/types.ail:62–70,88–101`; `src/core/dst_interaction.ail:59–95`), so the column-0 next-declaration rule is a starting heuristic, not a parser proof; verify comments/blank lines and EOF against actual AILANG parse before pinning (`P:426–451`). C-side missing/duplicate/moved and original-byte hashes are otherwise appropriate (`P:431–451`).

**P1.6.** The following independent-source cells need correction before implementation (`P:476–495`; `src/core/dst_driver_only.ail:1097–1123` takes identities as inputs; `scripts/dst/strict_replay_dst.ail:631–633` uses fixture literals):

| Field | Assessment at HEAD |
|---|---|
| source/scan revision; toolchain; generator | A's commit-object and assembled-tree hash is a different check from shell `rev-parse` (P:486); E's pinned binary SHA and generator commit are independent pins after P1G (P:487,494,602–605). But `git status --short -- ailang.lock` prints ` M ailang.lock`, so clean-tree admission must wait for a reviewed commit; P:486 says this correctly. |
| ABI/version/package | `ailang.toml:1–3` and `ailang.lock:71–77` both record 7.4, distinct files; the lock's `interface_hash` is **not** another ABI version and needs a separate fresh package-interface comparison (`P:488`). A package-directory tree hash without a pinned expected hash does not authenticate `ailang.lock`'s root (`P:491`; installed `internal/pkg/loader.go:125–136`). |
| classifier and unrouted | `tools/profile_definition/check_fixtures.py:54–59` `derive()` simply calls `tools/ext_call_inventory/derive.py --json`; P:489's two columns are the **same extractor**, not independent paths. `Makefile:3080–3082` target invokes derive.py without `--json`, so `make ext_call_inventory --json` is not the named JSON command. Use `python3 tools/ext_call_inventory/derive.py --json` plus an independent source/parser or reviewed hand-pinned fixture and compare sets. |
| scan roots | Inventory `scan_roots` defaults to `src,packages` (`tools/ext_call_inventory/derive.py:477–482,575–582`; `src/core/dst_driver_only.ail:1065`), whereas `SCAN_FILES` is four specific leaf files (`tools/driver_leaf_inventory/derive.py:187–192`). P:490 compares different kinds; compare roots to the profile's `scan_roots` and check leaf files lie inside them. |
| normalized config; profile/rule/vocabulary | `gen_fixtures.py` is not present at HEAD and can be an independent reader once P1 builds it (P:72–76,492). `validate_manifest` compares against the same profile builder and `check_fixtures.py` mainly checks classifier/ABI, not an independent truth source for every profile/rule version (`src/core/dst_profile.ail:1535–1573`; `tools/profile_definition/check_fixtures.py:738–750,806–945`; P:493). Name the source version functions/files and a separate extractor per version or narrow the claim to consistency. |

**P1.9a.** The installed binary/source is `ailang` v0.33.0 commit `ae36986c5c0a1e8ad4f0fc1e607efddefbbae46a` (`ailang --version`; `git -C /home/motoko/.local/share/ailang rev-parse HEAD`); repo `ailang/` is `de5a141e4df82766e4632f041654565409bbca66` (`git -C ailang rev-parse HEAD`), so P:922–924's attribution of `ae36986` to the repo `ailang/` source is false. Cite/inspect the installed source for the executing binary. Its cache override exists (`/home/motoko/.local/share/ailang/internal/pipeline/cache_store.go:47–68`); a fresh empty directory means no lookup hit **if** caching is enabled, but `cfg.NoCache` or `NewCacheStore` error bypasses it and `Save()` error is ignored (`pipeline_module.go:212–220,233–290,403–409`). `compile/manifest.json` stores successful cache entries, not an unconditional audit of the loader; it exists only if cache setup/save succeeds, and its module-ID set is not resolved file paths (`cache_store.go:84–105`; `pipeline_module.go:351–370`). Fail closed on missing/failed manifest, require active cache and zero observed hits with `-debug-compile`, and match the set to an actual loaded/compiled-module inventory with canonical id **and** resolved `File.Path`; lexical import statements alone miss entry prelude injections and module-prefix remapping (`loader.go:279–292,303–315,207–240`; `pipeline_module.go:95–118,122–179`).

`-trace-loader`'s `[trace-loader] Checking:` lines enumerate **candidate stdlib search paths**, not an explicit complete module-id→selected-path report (`installed internal/loader/stdlib_resolver.go:180–196`; P:563–567). `loader.go:176–196` can fall back to `<embedded>/std/`; binary hash is a sound identity for embedded bytes, but filesystem stdlib/package/project code needs selected real paths and per-file hashes. Package imports use the lock-backed resolver (`loader.go:145–157`; `internal/pkg/loader.go:44–69,125–136`): `ailang.lock:71–77` does pin the ABI package to the original checkout's **absolute** path, and the loader uses that path unchanged. P:571–573's refuse-outside-assembly rule detects this, but supplies no admissible parent assembly: the original path is neither `runs/<run-id>/tree` nor E's pinned copy. Pin evaluator-owned package/lock inputs, copy package dependencies into an assembly-owned root, and make an explicit, reviewed **assembly-only** relocalized lock/compatibility patch with original and effective lock hashes, resolved realpaths and package hashes; keep C/E source unchanged and make K0 compare the actual effective lock and loaded files (`P:478–495,551–575`; `ailang.lock:75`; installed `internal/pkg/loader.go:125–136`). Without that, P, C and control would all refuse at K0 rather than exercise K1–K7.

## §6 — P3.2

P:675–680 now uses P2.2's proposed tolerance on P and control under the guard in the same session, with entry counts pinned, and makes the final `make journal_replay_budget` rejection a P3G obligation after P3.3 exists (`P:633–654,686–703`; G:`P3.2,P3.3,P3G` deps). The four same-basis obligations still require reviewed resource-only intent, unchanged protected pins/raw outputs, K1–K7 equality and **prospective** allocation failure; K0 refusal does not count (`P:662–680`; A:665–676,709–720). The one longer dev/exposed entry ≤150 calls inside P3.2 is bounded and does not depend on P5 (P:681–684; G:`P5` deps P3G), but it exceeds P2.2's ≤100-call calibration scope (`P:633–654`; A:931–947). Before its provisional rejection counts, profile P/control at the longer length under the guard, record its peak/count/tolerance basis and exposure rows, and have P3G pin that length/threshold; otherwise an uncalibrated 150-call measurement is compared to a ≤100-call tolerance (`P:675–703`). The one extra admission/profile/repeat adds up to one day beyond P:773's base control estimate; state the conditional range in §7 (§8).

## §7 — Graph ↔ plan and estimates

`dagr check .dagr/run-plan004.json --strict --json` → exit 0, exactly `[]`; captured `graph_observed.json` gives 40 unique task ids, 14 events, `QRET queued`, `PLANW review`/three attempts, `REV1 done`/three, `REV2 working`/two, `PLAN1G queued`, `P6 canceled` (Python task/event inventory; captured SHA-256 in header). Each P:208–723 named part has one task including `PLANW`, four split sets and P6; task owners match P:91–100,851–853's conventions (`claude` implementation/docs/tests, `codex` reviews, `operator` questions/gates/P4/P6, `motoko` P1.1G; Python task inventory). Every active task has criteria, P6 alone is canceled/criterionless, and policy appears only on G:`PLANW,V5,PSYNC,P1.1`, the early author tasks (Python policy inventory; P:124–157,721–723). P:145's blanket “every author task” is inaccurate for G P1.2a–P1.9b; P:156's implicated-part/no-forecast exception must also say P1 parts settle on their own receipts before P1R and preserve prior `done` attempts on aggregate RETURN (§2).

Deps equal the stated part Deps and gate fan-ins: G:`PLAN1G` ← `PLANW,REV1,REV2` (P:215–223), `V5G` ← `V5,V5R` (P:277–282), `PLAN2G` ← `PSYNC,PREV` (P:284–300), `P1.1G` ← `P1.1,P1.1R` (P:325–329), `P1G` joins all P1 parts plus P1R (P:592–605), `P2.2` ← `P1G,P2.1,QRET,SCAN0` (P:633–635), `P3G` ← `P3.1,P3.2,P3.3` (P:697–703); Python dep inventory confirms. G:`PSYNC` now directly depends on `PLAN1G` as well as `V5G`, making REV1/REV2 a gate precondition (P:284–286). Gate criteria say accepted author/review plus receipts, consistent with P:124–157,215–223,281–300,325–329,592–605,697–703; dagr lint does not evaluate verdict prose. Current queued no-dep set is only `QRET`; `PLANW` is in review and REV2 is working, so PLAN1G waits. Once PLAN1G actually settles, G deps enable `SWEEP,V5,P2.1`, plus QRET if unanswered (Python deps inventory; P:201–202,244–266,611–633). P:187–199's skeleton now shows P1.6→P1.8 but draws `PLANW→REV2` as if a formal edge; G:`REV2` has `deps:[]` so its review can open while PLANW is `review` (G:`PLANW,REV2`; P:130–134). Annotate that informal submission arrow.

The **base** arithmetic is correct: P:771 part days sum 18½–22; P:768–774 totals sum 30–37½ to P3G (`python3` arithmetic; P:169–182,764–774). P:773 also says the one escalation adds up to one day, so an escalation makes the **conditional maximum 38½**, not 37½ (`P:681–684,773–774`; base max 37.5 + 1 = 38.5). This excludes extra review/fix rounds as P:181–182 states.

## §8 — New errors and precise corrections

| v1.2 claim | Correction and evidence |
|---|---|
| P:420–430 says import-region and each statement are spans but any overlap is exit 2 | Those spans overlap by construction; exempt deliberate region containment or use one aggregate import span. Include braced type `deriving` after `}` (`src/core/types.ail:81–86`; §5). |
| P:421–423 source examples | Multiple imports on one line are `src/core/test/stub_step.ail:44`, not :39–41; the `src/core/ports.ail:60–67` import ends at 67, not 70. |
| P:489 claims two independent classifier paths; P:490 compares scan roots with SCAN_FILES | `check_fixtures.py:54–59` invokes the same derive.py as the collector; `ext_call_inventory/derive.py:477–482,575–582` roots are `src,packages`, versus four leaf files in `driver_leaf_inventory/derive.py:187–192`. Use a different set extractor and compare like kinds (§5). |
| P:553–566 treats cache manifest-ID set and std search trace as actual complete loader evidence | Cache setup/save can silently fail and trace prints candidate paths only (`installed pipeline_module.go:212–220,403–409`; `stdlib_resolver.go:180–196`). Require selected `File.Path` inventory, active cache, manifest and zero hits (§5). |
| P:568–573 detects the absolute ABI lock path but “rewrites nothing” | Installed package loader honors that absolute path (`ailang.lock:75`; installed `internal/pkg/loader.go:125–136`). The current parent assembly fails P:572's own condition. Supply a pinned, assembly-only relocalization rule or another reviewed package root (§5). |
| P:922–924 labels repo `ailang/` as source of installed `ae36986` | `git -C ailang rev-parse HEAD` → `de5a141…`, installed source/binary → `ae36986…`; cite the installed source coordinates (§5). |
| P:773–774 includes a one-day escalation but to-P3G maximum remains 37½ | Base max 37½; with that extra day, conditional max 38½. Calibrate the >100-call escalation separately before pinning a threshold (`P:633–654,681–684`; §6–§7). |
| P:796–798 repeats `producer=` in its receipt and assumes producer file before first Delegate | Label `producer_run` and `producer_state`; use provisional `absent` before first delegate, then a linked first-reply receipt (`dagr.ail:269–284`; P:815–821; §3). |
| P:145 says policy on every author, and P:187 skeleton suggests REV2 deps PLANW | Only G:`PLANW,V5,PSYNC,P1.1` carry policies; G:`REV2` has no dep, so document P1R's exception and mark the arrow as submission (`python3` policy/deps inventory; §7). |

## Verdict for PLAN1G

**ACCEPT WITH CORRECTIONS.** The operator may settle `PLAN1G` **only after** the following exact contract edits are applied to v1.2 (and any affected operator graph criteria), strict graph lint is rerun, and each edit plus this review's digest is named in the gate receipt; a directive cannot substitute for the accepting `REV2·a2` verdict (`P:220–223,151–153`; G:`PLAN1G`; `dagr check .dagr/run-plan004.json --strict --json` currently → `[]`). This ruling permits only provisional SWEEP, V5, P2.1 and, after SWEEP, P1.1; V5G/PLAN2G still decide D1/D3/D5 and real entries (`P:15–29,201–202`).

1. In §0.10/§8, say explicitly that an early reviewer may be opened **manually** once the author is in `review`, even though its queued row `waits author`; the gate continues to depend on both. Separate P1R aggregate review: each P1 part first settles `done` on its own implementation receipt so downstream deps work; on P1R RETURN preserve prior `done` attempts and append `sent_back` attempts only for implicated parts, then require latest part `done` and latest P1R ACCEPT at P1G. On a third-RETURN unblock require a directive receipt, remove the consumed `·ask` future, and update the authorized round limit/current forecast in one checked mutation. The validator alone permits an unapproved a4 (`transaction_checks.txt:00a–08`; `09_aggregate_return_preserve_done.json` and `06_unapproved_a4.json` checks → `[]`; P:130–157,592–605).
2. Correct the first two-file receipt to `producer_run/producer_state/plan_state`; permit `producer=absent` only before first Delegate and require immediate first-reply linkage plus subsequent actual producer receipt. Keep the operator evidence-mismatch ruling and fresh marker checks (`P:796–808,815–821`; `dagr.ail:269–284`; §3).
3. Make M15 explicitly **provisional for V5's D8 ruling**. Replace the wrong-run Association and edited-program cases, condition A3 cases on a next continuation, individually justify the decoding/cutoff inapplicables, and require each case's preflight/earlier-check pass and first position. Add separately frozen expected/observed finding and position columns to `MATRIX.tsv`. V5G must rule D8 wording before P1 starts (`P:266–271,331–383`; A:683–733; §4).
4. In P1.4b allow intentional import-region containment and protect trailing `deriving`; correct its source coordinates. In P1.6 replace the duplicate classifier extractor and wrong scan-root comparison, specify an independent comparator for each remaining identity or narrow its claim, and use `python3 tools/ext_call_inventory/derive.py --json`. These are contract edits before their implementation, still held by PLAN2G (`P:414–451,476–495`; source evidence §5).
5. In P1.9a require active cache/miss and selected-path evidence instead of treating `manifest.json`/`Checking:` as a complete loader trace. Define an evaluator-pinned, **assembly-only** lock/package relocalization with effective hashes so P can run; cite the installed `ae36986` source rather than repo `ailang/` (`P:553–575,922–924`; installed source and `ailang.lock:75`; §5).
6. For P3.2's >100-call escalation, profile a fresh peak/tolerance/count scope under the guard before counting control rejection, and show the conditional to-P3G maximum as 38½ delegate-days with that extra day. Annotate §1's REV2 arrow and §0.10's P1R policy exception (`P:633–654,681–684,773–774,187–199,145–157`; §6–§8).
