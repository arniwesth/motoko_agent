# PLAN-004 v1 and its dagr graph — adversarial review

Date: 2026-09-15. HEAD: `39208145b7b039500b6639b7e70e476e91bab288`. Branch: `arniwesth/013-plan003-and-herdr`.

Subject: `PLAN-004-implement-adr-004.md` proposed v1 and `.dagr/run-plan004.json` contract v3. Inputs: `ADR-004-journal-as-evaluation-source.md` proposed v4; `REVIEW-adr004-v4-verdicts-codex.md` (RETURN for v5); `PLAN-003-implement-adr-003.md` §0, P4 layout and §5; `.dagr/run-plan003.json`; `HANDOFF-2026-09-12-plan003-p1-and-the-delegation-view.md` §6; `DESIGN-dagr-as-delegation-view.md` §10.5–10.5.1. **P**, **A**, **R**, **G**, **D** below mean those first, second, third, graph and design files; code coordinates are at HEAD. `git rev-parse HEAD` and `git branch --show-current` returned the identities above. `git status --short` showed pre-existing modifications and untracked work; none was touched.

Section shorthand in R resolves to these file:line headings: §3.4 :116, §3.5 :122, §3.6 :130, §3.7 :157, §3.8 :169, §3.9 :194, §3.10 :210, §3.11 :229, §4/§4.1 :235, and “Required changes for v5” :282 (`rg -n '^### 3\.|^## §4|^## Required' R`). Graph task-name shorthand resolves to the task-id lines indexed in §7.

Method: read the governing ADR and review, inspected all plan parts and task definitions, checked the cited HEAD coordinates and APIs, compared `de4b4f5`'s reverse patch with the candidate class, and ran `dagr check .dagr/run-plan004.json --strict --json` (exit 0, output `[]`). Baseline `ailang check` on `stub_step.ail`, `session.ail`, and `ledger_parity_dst.ail` each exited 0 with “No errors found.” No sweep, replay, prototype, profiling, corpus-content read, pane operation, agent operation, commit, or repository edit other than this document. Temporary evidence directory: `/tmp/claude-1001/-workspaces-motoko-agent/d8b1d53b-1a2d-4c49-b4ec-fc412754e86a/scratchpad/codex-review-plan004/` (created). Prior v4 evidence remains described in R:1–7, §§3.9–3.11; its private scan output was not copied here.

## Overall verdict — RETURN for v1.1

The early-work split is mostly defensible as *provisional* work: preservation, a read-only scan, a standalone guard, the reviewed line-neutral variant and ADR revision do not require an answer to returned D1/D3/D5, provided no real-entry admission or protected pins are taken before v5 (`P:14–27,54–70,185–204,291–316`; `R:274–292`). The executable P1–P3 path must remain closed. As written, however, the graph does not make this v1 review a dependency of `PSYNC`, and §8 makes the producer rewrite its `dagr_plan` input. More seriously, the planned v5 re-grounding may edit task definitions already seeded into the producer's run file, which §10.5 says are **left alone**; a strict-clean input is not enough to make those revised gates govern delegation (`G:84–92,206–230`; `P:276–281,823–827`; `D:638–644,668–690`). P1's D8 test inventory and several later parts are also too loose to pass the stated gates.

## §1 — The central split

| Work before `V5G` | Verdict and evidence |
|---|---|
| `PRESERVE`, `SWEEP` | **Sound in principle.** D8 explicitly puts archival P0 first; ADR-001 D6 sweep precedes the only core boundary edit. Preserve copies no content into tracked files except path/hash manifest; `SWEEP` is a record, not a code change (`A:915–919,1010–1015`; `P:54–64,149–183`). The privacy/manifest mechanics need the corrections in §6. |
| `SCAN0` | **Sound only as a heuristic, read-only finding.** The body16 recount follows R change 8, including all 23 prefix occurrences, but v5 may revise the policy and a scan cannot admit an entry. `P:185–204` correctly says so. Do not make its 57-turn result an admission or use it to relax the policy (`R:246–249, §§3.10`). |
| `P2.1` | **Sound as an isolated guard.** D8 expressly assigns margin, poll period and unlimited-max treatment to PLAN-004; these defaults do not depend on D1/D3/D5. Its callers and calibration contract need specification, not a v5 answer (`A:931–938`; `P:634–660`). |
| `P1.1`/`P1.1R` | **Conditionally sound.** D2 was accepted with corrections and R:§3.9 gave the five joins. v5's D5 closure pins are at *admission A*, after this commit. The join does not decide the seam, manifest or candidate class (`P:291–327`; `R:§2,§3.9`). Gate P1.1R must explicitly accept the review before P1.5 and P1G can use it. |
| `V5` | **Necessary.** Nine returned changes are named at `P:206–253`; no D1/D3/D5-dependent evaluator work may start until a corrected ADR is accepted and this plan reviewed (`R:Required changes for v5`). |

The gate is also **overbroad for independent preparatory work**: source-coordinate audit, fixture inventory and checker design could be done read-only before v5, and D6's ignore/archive work already is. Keeping code for P1.2/P1.4–P1.9 behind `PREV` is prudent because v5 may change their contracts; it is not a missing opportunity to delegate implementation early (`P:23–27,288–289`; `R:Required changes 1–6,8`). P2.2 must additionally wait for the retention ruling, the final E, guard and scan finding (`P:662–682`; `G:P2.2 deps`).

## §2 — Fidelity to ADR-004 v4 and the nine v5 requirements

Disposition judges **what v1 carries forward**, not whether the returned v4 decision is already executable. Locations are P unless prefixed A or R.

| Input | Disposition | Where and evidence |
|---|---|---|
| D1 extraction | **PARTIAL** | P1.2/P1.3 reproduce selector, seed, excerpt, association, cutoffs and T0 table (`P:329–402`; `A:369–525`), but zero-call `tool_calls`, blank/hybrid and `StopBeforeEnd` remain ⟨v5⟩; `ParkOrWake` reason is corrected only provisionally (`P:375–386,112`). |
| D2 admission | **CARRIED** | Variant, journal world, guarded seams, traced admission, program `/4` and no new Ports field (`P:291–316,432–505`; `A:527–662`); failure outcome details remain properly gated as R change 5 (`P:460,485–503`). |
| D3 checks/verdict | **PARTIAL** | A1–A9/K0–K7, candidate witness and census appear (`P:507–540,573–610`; `A:663–760`), but obligation/meta, env fault paths, location and first-mismatch semantics remain ⟨v5⟩; test inventory omits named negative arms (§5). |
| D4 claims | **CARRIED** | Whole profiled process including seed fold, discard divergent measurements, prospective short calibration (`P:41–48,673–682`; `A:769–782`; `R:§3.11`). |
| D5 trust/identities | **PARTIAL** | E overlay, path/region refusal, assembly plus execution provenance and marker N/A are carried (`P:75–85,404–430,573–598`; `A:784–840`), but checker closure and control basis await v5 and the graph's later rewrite cannot reliably deliver new pins (§7). |
| D6 corpus | **PARTIAL** | Ignore first, private archive/entry, contextual scan, exposure and held-out process (`P:149–174,542–571,733–737`; `A:842–894`). Retention remains unanswered (`P:748–750`), and P4/P5 exposure rules lack enforceable graph criteria. |
| D7 ladder | **CARRIED** | T0 omissions and P6 `/5` work are only a later sketch (`P:394,522–530,741–742`; `A:896–904`). Graph incorrectly queues P6 as a normal implementation after P3G (§7). |
| D8 sequence/gates | **PARTIAL** | P0→P1→P2 calibration→P3 measured rejection→P4/P5/P6 is present (`P:118–143,612–742`; `A:915–952`), but D8's matrix is incomplete, P3.2 is unproven, and P1G/P3G do not bind an accepted review (§4–§5). |
| R change 1 (branch-complete calls) | **PARTIAL** | V5 lists all four repairs; P1.3 leaves three ⟨v5⟩ and names no separate zero-call/blank/hybrid/post-turn tests (`P:212–216,375–400`; `R:Required changes 1`). |
| R change 2 (env/context/fault reads) | **PARTIAL** | V5 lists pins/choice/counts; world/files and A7 mention them, but no env-value, profile-path, context-encoding or failure-run test is assigned (`P:217–222,390–394,442–445,522`; `R:Required changes 2`). |
| R change 3 (C witness) | **CARRIED** | K3 rebuilds from C trace/worlds; prepared-count mutation is named (`P:223–225,590–591,608`; `R:Required changes 3`). |
| R change 4 (obligation/meta/census) | **PARTIAL** | Separate budgets and census are named (`P:226–230,525–530,537–538`), but concrete `ReplayObligation`/`ReplayMetadata` are deferred and test must verify each zero input, not merely print rows (`R:Required changes 4`; `dst_execution.ail:100–125`). |
| R change 5 (failure fields/marker/location/provenance) | **PARTIAL** | All are listed for V5 and distributed to P1.5/P1.6/P1.9 (`P:231–235,460,489,594–605`); a marker-specific first-mismatch test and complete failure-field tests are absent (`R:Required changes 5`). |
| R change 6 (closure/checker/execution provenance) | **PARTIAL** | Original-byte hashes, closure and execution provenance are named (`P:236–240,416–429,583–586`), but manifest coverage, parser failure cases and actual import/cache linkage lack concrete criteria (`R:Required changes 6`). |
| R change 7 (control basis) | **PARTIAL** | V5 leaves both routes open; P3.2 suggests a same-basis reverse `de4b4f5` diff (`P:240–245,692–705`), but admissibility, protected pins and short-entry allocation failure are unproved (§4). |
| R change 8 (scan) | **PARTIAL** | SCAN0 cites four indices, P1.8 says heuristic/all occurrences/name report-only and refuses to relax (`P:187–204,246–248,546–553,667–670`); actual four tool-prefix report sites and component reason counts are not in V5's promised record, and tests omit the name/all-occurrences cases (`R:§3.10,Required changes 8`). |
| R change 9 (claim/citation audit) | **PARTIAL** | V5 says repair R:§4 (`P:249`), but v1 repeats two wrong prototype/control claims (§8) and no concrete corrected citation list is a gate (`R:§4`). |

The added split, checker paths, E worktree and guard defaults are PLAN-004 implementation choices, broadly licensed by D8/D5 (`A:806–813,931–938,1008`; `P:14–27,75–85,636–658`). They are marked overridable at `P:75,636–637,752–753`. `../motoko_agent-eval` is a reasonable local default if that worktree does not already exist or carry unrelated changes; §0.5 needs a collision check. Retention is **not fixed** despite D6 delegating it to PLAN-004 (`A:848–849,1008`; `P:748–750`). The suggested control is a new choice the ADR's historical-control sentence does not itself license; R change 7 permits a same-basis alternative only after v5 explicitly chooses and reviews it (`A:939–948`; `R:Required changes 7`).

## §3 — Code facts at HEAD

`wc -l` returned stub 915, session 6320, ports 2885, ledger parity 642, anchors 627, derive 695, Makefile 3299. Targeted `git show d5edebf:<file>` line counts were stub 901, ports 2835, session 6004, ledger parity 642. `git check-ignore .motoko/eval-corpus/example` exited 1, so the corpus is still unignored. `ailang check` of the three P1.1 files returned exit 0 on the **baseline**, not on an overlay.

| Plan coordinate/claim | Finding and HEAD evidence |
|---|---|
| Drift table `P:106–114` | Stub sum/recorder/ports/trace coordinates resolve: `stub_step.ail:69,478,635`; `ports.ail:1723,2056`; `session.ail:195,272,1532,1545,2760,3255,4423`; `ledger_parity_dst.ail:428–435`; `derive.py:154`. `ports.ail:1827` is exported `record_interaction`; current provider/exhausted/tool encoders are `ports.ail:2507,2522,2577`. The table's +46 is correct for the two named ports functions, but it is **not** a universal offset for every codec (`P:109`; `ports.ail:2507–2522,2577`). |
| `ParkOrWake` against `686da16` | `git show 686da16 --stat` identifies ADR-003 D7 P4 Part 1; `git show 686da16:src/core/journal.ail` shows `all_entry_types` with `park`,`wake` at :1253–1256. At HEAD they remain in `journal.ail:1253–1256`. Thus A:501's “ten types/no decoder” reason is stale; a park is a run boundary, not a usable T0 in-segment call (`P:112,357`; `A:482–502`). |
| Five joins | All five exact join sites resolve (`stub_step.ail:69`; `session.ail:195,1545`; `ledger_parity_dst.ail:73,435`; `P:296–303`). Same-line append before the stub comment preserves its constructor; session/ledger matches need the new arm. `scripted_ports.ail:30–35` remains the intentionally limited helper. Textually, only those five lines need change and line counts can remain unchanged. This review did **not** type-check a modified overlay; P1.1's gate must do that (`P:309–314`). |
| Every anchor/pin | `anchors.sh:593` also pins `stub_step.ail:203`'s ambient `now()`; `anchors.sh:614–617` pins session 1471/1730/1842/4302/4523 and `tool_phase.ail:484`. Targeted Python read returned `True` for all seven pin predicates. `derive.py:154` fixes `BRIDGE_SPAN=(session,1085,1474)`. Same-line edits at stub69/session195/1545 do not shift *any* of these lines or the bridge span; changed text is outside the bridge. P1.1R must compare the span bytes and run the gates after the real diff (`P:65–70,320–325`). |
| Cross-reference Make lines | `Makefile:350` is `ledger_parity:`, :697 `DST_KNOWN_RED := driver_plus_herdr herdr_graded`, :1099 `strict_replay:`, :1516 `event_vocabulary:`, :3071 `anchors:`, :3095 `driver_leaf_inventory:`. All six resolve (`P:847–850`). `step_machine.ail:102` is `call_model_or_fail`; `session.ail:1532`, not :1533, starts `ported_provider`. |
| P1.5 exported seam surface | `stub_step.ail:478` `recording_model_step` is **private**, :635 `recording_ports` exported; `ports.ail:1723` `world_tool`, :1827 `record_interaction`, :2056 `recording_tool`, :2522 `encode_exhausted_provider_outcome`, :2577 `encode_tool_outcome` are exported. `ports.ail:2126` `tool_outcome_record` is private. Nonempty guarded delegation therefore fits the public surface; empty arms must construct complete `TimedOutcome` themselves (`P:446–461`; `A:567–610`). |
| P1.6 entry/manifest | `session.ail:4423–4442` does accept model/history/step budget/`StepProvider` and normalises the provider **before** policy init; it has no explicit prior-count argument. `dst_profile.ail:1535–1573` validates required fields, profile id/version and rule versions, not whether source/toolchain/scan identities are true; `dst_driver_only.ail:1097–1123` provides a builder with *caller-supplied* revision/toolchain/config/derived sets. P1.6 must derive those inputs and test a wrong but nonblank provenance value via independent preflight (`P:477–496`; `R:§3.5`). |

No numeric coordinate in P:§0.9, P1.1 or P:Cross-references is out of range. Two citations need tighter semantic use: `session.ail:1545` is an arm, while the function begins :1532; `Makefile:697` states a known-red *list*, not proof that both targets were red in a new sweep (`P:56–57,102–114`; `R:§4.2`).

## §4 — Per-part verdicts

`ACCEPT WITH CORRECTIONS` means scope can remain one delegate/commit; `RETURN` means the part's contract or gate must be rewritten before delegation. Evidence is P unless another source is named.

| Part | Verdict | Review |
|---|---|---|
| PRESERVE | **ACCEPT WITH CORRECTIONS** | Half-day archive is bounded; ignore-first and hashes are real (`P:149–174`; `A:917–919`). Make tracked manifest paths relative and ensure it contains no private naming beyond allowed identities; audit permissions on **all** copied parents, and handle missing sources/collision without clobber. `sha256sum -c` must run from the archive root. |
| SWEEP | **ACCEPT WITH CORRECTIONS** | Correct boundary precondition and no commit (`P:54–57,176–183`); gate must compare the closing summary to actual exit/reds, record even listed-and-passing targets, and stop P1.1 on a new unexplained red. `DST_KNOWN_RED` is only a register (`Makefile:697`; `PLAN-003:1186–1219`). |
| SCAN0 | **ACCEPT WITH CORRECTIONS** | Read-only/½ day and after preserve is reasonable (`P:185–204`; `G:70–81`). Count every site/reason including four tool-prefix reports, specify what data was absent, and migrate this agent's pre-entry exposure to the eventual log. Its gate is a count/report, never admission. |
| V5 | **RETURN** | Nine changes in 1–2 days may be optimistic given new mechanism and claim audit (`P:206–253,764–766`; `R:Required changes 1–9`). Gate needs *decision text plus tests/contracts*, not just nine retraction headings. First v4 commit should not accidentally include unrelated untracked files (`P:251–253`; `git status --short`). |
| V5R | **ACCEPT WITH CORRECTIONS** | Read-only independent review is appropriate (`P:255–267`). Reviewer identity and output must be checked, and RETURN must be a blocking result rather than merely another completed task; loop policy has no clear exhausted-round disposition (§7). |
| V5G | **ACCEPT WITH CORRECTIONS** | Operator gate correctly requires no RETURN, ruled corrections and Accepted status (`P:268–272`). Require the review document and directive receipt, and gate on *actual* revision round, not the first V5R attempt. |
| QRET | **RETURN** | D6 assigns retention to PLAN-004, but the question has no default/duration/deletion rule and null criteria (`P:748–750`; `A:848–849,1008`; `G:QRET`). Fix a reviewed default or an explicit operator ruling that covers archive, scan0, entry, runs and other copies **before** copying real content. |
| PSYNC | **RETURN** | Replacing markers and rechecking scopes is right (`P:274–279`); add `REV1` and explicit accepted v5-review dependencies, and solve seeded-task immutability before editing graph (§7; `D:640–644`). A 1-day rewrite can be reviewable only if deltas and task identity changes are audited. |
| PREV | **RETURN** | A read-only review is appropriate (`P:280–281`); “P1 may start (possibly with corrections applied first)” is not a gate. Require ACCEPT or a named correction round re-reviewed/ruled by operator before P1.2/P1.4 can be ready (`G:PREV criteria`). |
| P1.1 | **ACCEPT WITH CORRECTIONS** | One five-line core commit/½ day is realistic and feasible textually (§3; `P:291–316`). Test changed overlay and all pins including stub203; `git diff --stat` alone does not prove exactly five changed lines. No red-first test is claimed here. |
| P1.1R | **ACCEPT WITH CORRECTIONS** | Independent review is essential (`P:318–327`); gate needs an **ACCEPT** verdict and byte comparison of bridge span, not merely “verdict in §6” (`G:P1.1R criteria`). |
| P1.2 | **RETURN** | D1 reader is a large 1½-day single module with selection, strict fold, two-source association and 11 refusals (`P:329–369`; `A:390–424,494–502`). The decoder copy/hash relation to A and v5 branch contract must be explicit; tests need both refuse and clean counterpart for each refusal family, plus duplicate/ambiguous and park boundary cases (§5). No demonstrated red-first sequence. |
| P1.3 | **ACCEPT WITH CORRECTIONS** | Separate stopping/config module is sensible (`P:371–402`), but add tests for zero-call `tool_calls`, blank, hybrid native-seed, post-turn stop, profile/env/context failure and accepted end, and make each cutoff test assert the resulting selector/N/end. v5 determines expected behavior. |
| P1.4 | **RETURN** | Digests plus a protected-source parser/manifest/CLI and five mutation self-tests is too much for 1½–2 days (`P:404–430`). `derive.py:63–86` strips quoted text/comments while preserving offsets; using it to locate spans is only a starting technique. Define nested/interpolated string handling, exact span boundaries, duplicate/missing symbol failure, transitive import/type/helper closure and overlaps. Hash original bytes; decide comment-only treatment consistently with that rule. Independent manifest generation and check of a mutated copy are needed so tests do not mirror the checker. |
| P1.5 | **ACCEPT WITH CORRECTIONS** | Guarded delegation is achievable through the exports (§3; `P:432–471`). Test complete provider and tool failure `TimedOutcome` (payload/status/class/chunks/advance/deadline), correlation and no-process empty queue; assert exact log-prefix plus one appended interaction and preservation of every other field. `world_state_of` equality alone does not test the widened projection. |
| P1.6 | **RETURN** | Traced entry and `/4` program use existing functions, but “real identities” is unspecified and `validate_manifest` accepts nonblank invented provenance (`P:473–505`; `dst_profile.ail:1535–1573`). State how policy file path/values, generator version, source revision, scan sets, toolchain, binary and profile derive; distinguish `validate_program` from provenance preflight. Add wrong-identity and malformed/end/framing tests. This can be one commit after the contract is concrete; 1½–2 days is optimistic. |
| P1.7 | **RETURN** | A1–A4/A7/A8 and finding map in 2 days are substantial (`P:507–540`). Specify candidate-versus-admission comparands, independent source fold, all-zero and fault env expectations, concrete obligation/meta and the sub-obligation census; assign negative A1/A4/A8 tests and location tests (§5). A census that only prints zeros is not an invariant test. |
| P1.8 | **ACCEPT WITH CORRECTIONS** | Scan/artifact/entry is reviewable in 1–1½ days if the policy is pinned (`P:542–571`). Tests need JWT, URL userinfo, body16 on a later prefix occurrence, `CredentialBearingName` report-only, each component and a derived-output refusal; validate permission failure paths and no report body. No second codec is licensed (`A:929–930`). |
| P1.9 | **RETURN** | Two-day runner has worktree assembly, three preflight refusals, K0–K7, provenance and typed verdict (`P:573–610`). Specify who owns the candidate worktree/record, prevent source import from bypassing E, verify execution provenance against actual resolved imports/cache, and forbid scoring after any K failure. Tests must separate candidate-path/intent/evaluator/protected/manifest/guard/program refusals and directly create each `ReplayMismatch` variant; an exhausted marker does **not** entail `ProgramExhausted` (`dst_replay.ail:319–386`; `R:§3.4`). |
| P1G | **RETURN** | The criteria are directionally correct (`P:612–628`) but “matrix complete” has no inventory/result artifact and §5 shows gaps. `ailang test` all modules plus checker and anchors are real commands; grep cannot prove absence of a second comparison/reconstitution/codec/manifest by itself. Require code review with symbol inventory and accepted P1.1R/PREV. |
| P2.1 | **ACCEPT WITH CORRECTIONS** | One-day Python guard and fake-cgroup tests are plausible (`P:634–660`). Require declared peak with units/source for *every* caller, handle nonexistent/malformed/zero `max`, check child process group reap after TERM/KILL and `guard.json` atomicity, and make normal-exit test assert caller rejects `trip:true`. Self-tests must use a scratch lock so P2.1 before PRESERVE does not create an unignored corpus file. |
| P2.2 | **RETURN** | Heavy calibration is a record exception, not one delegate commit (`P:662–682`). It assumes “25 and 57-turn prefix” can be CutoffBefore/SourceFaithful without proving an admissible selector, and mutant divergence is not the D8 known **refusal** or allocation regression. Specify profile command/measurement scope and independent count pins, warm-up and exposure, and a stop condition if no dev entry passes. Dependency on SCAN0 is sensible. |
| P3.1 | **ACCEPT WITH CORRECTIONS** | Admit one finite dev entry under guard after calibration (`P:684–690`); ½–1 day is plausible only if P1 admission/scan are complete. List it as a no-code-commit exception, record every refused candidate and agent exposure, and make the gate A1–A9 plus permissions/identity checks mechanical. |
| P3.2 | **RETURN** | The pass condition is correct (`P:692–705`; `A:939–948`; `R:Required changes 7`), but proposed reverse `de4b4f5` control is **not automatically admissible**. `git show de4b4f5 -- src/core/phase_vocab.ail` changes imports and `canonical_messages`, not raw message inputs; D5 leaves `phase_vocab.ail` unprotected (`A:803–804`), while D3 refuses encoding changes unless reviewed as an output-byte-identical resource-only candidate (`A:665–676`). The *protected* basis may equal A, not the assembled source identity. Historical 299-step evidence does not prove a finite ≤100-call control fails the final budget. v5 must choose, review the diff/basis and prospectively show K1–K7 equal observations **before** counting allocation failure. |
| P3.3 | **ACCEPT WITH CORRECTIONS** | A one-day target after calibration/control is bounded (`P:707–722`). Prove the Make target invokes guard for parent/control/candidate, pins each observation count from final entry, and rejects missing provenance/scope/trip. A reason-only repin record is insufficient without operator review of a basis change (`A:836–840,939–948`). |
| P3G | **ACCEPT WITH CORRECTIONS** | Correct measured gate (`P:724–729`), but give it command receipts and explicit exit/measurement comparison, not a narrative claim. ADR “measured” line should cite finite-entry results, not prototype values (`R:§3.11`). |
| P4 | **RETURN** | Operator ownership and hidden pins are correct (`P:733–737`; `A:889–894`), but graph has no criteria. Define exposure classification, collection authority, per-entry refusal reporting and held-out storage protection before declaring done; Motoko must not see entry identities/results. |
| P5 | **RETURN** | Long admission after P3G and guard is correct (`P:738–740`; `A:949–951`), but no gate, dev/held-out separation or exposure criteria exist in graph. A 300-call-class run may take longer than 1–2 days if admission or memory trips; old RSS is only prototype scale (`R:§4.1`). |
| P6 | **RETURN** | Explicitly unscheduled and needs a new ADR/plan (`P:741–742`; `A:896–904,952`); graph's queued impl node becomes ready after P3G with null criteria. Make it a placeholder blocked on that later decision, or remove it from this run. |

P1.2–P1.9's tests are described as “red first where a test can be red” (`P:288–289`) but no red-first commit or baseline failing result is specified; unlike PLAN-003 P4 Part 1's named red-first cases (`PLAN-003:694–705`), most are new modules whose tests could be written after implementation. Require a recorded failing mutation or pre-implementation test where meaningful, and do not credit mere asserted ordering. None of the parts expressly builds D8's four forbidden duplicates; P1.4 digest copies and P1.2 decoder copies are licensed evaluator copies (`A:806–810,929–930`), while `P1G` must audit that they do not become a second comparison walk or persistence codec (`P:623–624`).

## §5 — D8 P1 matrix: coverage and gaps

The union of stated tests covers a clean journal, leading-user seed, both association directions, each named cutoff in P1.3, both end shapes, both seam empty arms, correlation, witness under/over, a private-key scan refusal, C=P, a projection divergence and six additional replay variants (`P:363–367,396–400,463–469,498–503,533–538,565–569,600–608`; `A:926–930`). Coverage is **promised**, not demonstrated; P1G has no case→test table yet (`P:614–620`). The following must be assigned tests with assertable outcomes:

1. **Refusal families.** D1's `ChainBreak`, `MalformedEntry`, `UnknownEntryType`, `UnknownSchema`, `DuplicateOrAmbiguousCallId`, `UnexpectedToolResult`, `MalformedToolArguments`, `Association`, `ContinuationStart`, `ParkOrWake`, `EmptySegment` have one proposed *refused* journal each (`P:353–364`; `A:494–502`), but no paired known-divergent/known-clean boundary case per family. Candidate `InadmissibleCandidate` by path **and** intent, `EvaluatorTouched`, `ProtectedRegionTouched`, `PreflightMismatch`, `GuardTripped`, `ProgramUndecodable`, scan reasons and `AdmissionCheck(A1..A9)` are not each inventoried as known-refused plus divergent where meaningful (`A:671–691,727–734`; `P:565–569,600–608`). Clarify D8's “per refusal family” scope before closing P1G; a refused preflight does not count as a divergent replay.
2. **`ReplayMismatch` variants.** All seven names are assigned across P1.9's projection test and six “not already covered” tests (`P:600–605`; `A:736–739`). But `ProgramExhausted` and `UnusedInteraction` need direct expected/actual length mutation tests; an exhausted marker can yield an earlier different mismatch (`dst_replay.ail:357–386`; `R:§3.4`). Require each assertion to identify the first finding and position.
3. **Cutoffs/stopping.** P1.3 says one per cutoff, but the returned zero-call `tool_calls`, blank completion, hybrid seed predicate and post-turn `StopBeforeEnd` selector/N/end are not individually named (`P:375–400`; `R:Required changes 1`). `Completed`, `Exit`, EOF and incomplete batch must assert whether end is genuine or synthetic and the exact shortened N (`A:478–492`).
4. **Seams/fail closed.** Both empty arms are assigned (`P:463–469`); missing are a complete fatal provider failure `TimedOutcome`, a tool failure/retry classification, marker-first-mismatch test, and proof the tool empty arm never calls `world_tool`'s live branch (`ports.ail:1723–1740,2056–2072,2126–2140`; `R:Required changes 5`).
5. **Source/checks/census/scan.** There is no negative A1 independent fold, A4 altered argument, A5 malformed frame, A8 wrong obligation/meta, A9 wrong-but-nonblank provenance, fault-path env read, all sub-obligation-zero assertions, or tagged aggregate finding-location test (`P:493–503,509–538`; `R:Required changes 2,4,5`). P1.8 lacks JWT/URL/body16 later occurrence/name-axis and component scans (`P:546–569`; `R:Required changes 8`). Candidate prepared-count mutation is correctly named (`P:608`; `R:Required changes 3`).

The P3 allocation control is **separate** from this synthetic P1 matrix; D8 requires the finite dev-entry control to preserve every observation before failing allocation (`A:939–948`; `P:701–702`).

## §6 — Standing rules and safety

The sweep before the boundary edit, no real replay before the guard, one heavy run, `<12 GiB` preflight, unchanged schema/Ports/recorder projections, and no re-baseline are explicit (`P:52–74,815–821`; `A:931–938`). The sweep is not run by this review. A delegate can check `memory.current`, but a statement that no other heavy task exists is a coordination assertion; the guard's `flock` should be the enforceable run-level rule (`P:61–64,639–650`). `P2.1` starts before PRESERVE: self-tests should not instantiate `.motoko/eval-corpus/.heavy.lock` under an unignored path (`P:154–155,634–658`). The 2 GiB margin, one-second poll and unlimited-max refusal are reasonable conservative defaults, explicitly overridable, but a sampled monitor is not proof against instantaneous cgroup OOM; any trip or missing monitor/record must invalidate the result (`P:636–651`; `A:931–937`).

Privacy rules correctly exclude corpus payloads from briefs, commits, graph notes and records; counts/digests/indices/identities only, with 0700/0600 corpus permissions and derived output under runs (`P:86–92,154–172,557–563`; `A:842–861`). Fix the pre-entry SCAN0 exposure migration and make every warm-up/profiling agent an exposure-log row (`P:90,201,671–675`; `A:860–861`). P4 is operator-owned because Motoko orchestrates candidate-producing work; graph P4 says so, but §8's blanket “never name a held-out entry to anyone” should say **candidate-producing agents**. P5 must not hand a held-out entry to Motoko as a long-entry delegate, or it ceases to be held out (`P:91–92,733–740,808–810`; `A:889–894`). Graph null criteria for P4/P5 give no mechanical exposure gate (§7).

The commit rule's stated exceptions are incomplete: `P:93–95` names SWEEP/SCAN0/P2.2 and V5, but P1.1R, V5R, PREV, operator gates/questions, P3.1 admission and P5 long admission are review/record/decision work without a normal one-code-commit scope (`P:255–281,318–327,684–690,724–742`). Say which produces no commit and which produces a reviewed document/control commit. Records should have command output, exit, HEAD and reason as PLAN-003 §5 does (`P:755–758`; `PLAN-003:1186–1219`). §8's “verify gate before settling” is right, but a returned review or unrun gate must not become a satisfying dependency (`P:811–813`; `HANDOFF:175–178`).

## §7 — The dagr graph

`dagr check .dagr/run-plan004.json --strict --json` exited 0 and printed exactly `[]`. A targeted `python3` JSON inventory printed `dagr 3`, `tasks 30`, `duplicate_ids False`; queued tasks with no deps: `PRESERVE,SWEEP,V5,QRET,P2.1`; working with no deps: `REV1`; null criteria: `QRET,P4,P5,P6`. Thus §1/§8's **queued ready set** is correct, with the existing review in progress also independent (`P:143,793–796`; `G:44–92,206–230`). There is exactly one node for each named plan work item plus REV1, but no plan §1 edge out of REV1; its review is operationally optional (`G:84–92`; `P:129–143,274–281`).

At HEAD, `rg -n '"id":' .dagr/run-plan004.json` gives this task-id coordinate index for all `G:<task>` citations: PRESERVE:46, SWEEP:58, SCAN0:70, REV1:84, V5:154, V5R:166, V5G:199, QRET:212, PSYNC:223, PREV:237, P1.1:251, P1.1R:265, P1.2:279, P1.3:293, P1.4:307, P1.5:321, P1.6:337, P1.7:351, P1.8:365, P1.9:379, P1G:394, P2.1:415, P2.2:427, P3.1:444, P3.2:458, P3.3:472, P3G:486, P4:501, P5:514, P6:527. Each id begins the task object whose deps, criteria and owner are cited.

Deps mostly match the written work: SWEEP→P1.1→P1.1R, V5→V5R→V5G→PSYNC→PREV, P1.2/P1.4 branches into P1.5, P1.7/P1.8 into P1.9, all nine P1 nodes into P1G, and P1G/P2.1/QRET/SCAN0 into P2.2 (`P:129–141,288–289,434,544,664`; `G:task deps`). P1G's nine-input fan-in equals its advertised P1 review scope; P3G's three inputs equal P3.1–P3.3 (`P:612–629,724–729`; `G:P1G,P3G`). Direct edges from P1.2–P1.8 to P1G and P3.1–P3.2 to P3G are redundant transitively but useful as explicit gate fan-in. Missing edges/conditional transitions are material:

- **`REV1 → PSYNC`** (or an operator ruling node) is required so this review's corrections cannot be skipped (`G:REV1,PSYNC`; `P:274–279`). The graph currently treats REV1 as isolated.
- **Accepted `P1.1R` and `PREV` verdicts** must govern P1.5/P1G and P1.2/P1.4 respectively. Current criteria only require a verdict or say P1 may start “possibly with corrections applied first”; `done` can satisfy deps even after RETURN (`G:P1.1R,PREV,P1.2,P1.4,P1.5`; `P:318–327,280–281`). Add explicit gates or revision loops.
- `V5R`'s `rounds_max:3` and fail future `V5·a2` are sensible bounds for another ADR round, but the future's fixed id/title “v6 revision round” and loop-back do not say how revised v6 is re-reviewed, how the *latest* verdict feeds V5G, or what happens after three returns (`G:V5R policy`; `P:255–272`). A returned review cannot simply be settled `done` and allow V5G.
- P4/P5/P6 have null criteria, and P6 is queued despite P:741–742's “unscheduled.” QRET has null criteria despite blocking real-content copying (`G:QRET,P4,P5,P6`; `P:748–750`). Owner `motoko` for impl/test/docs nodes conflicts with §8's `kind: claude` delegation and run-plan003's delegate-owner convention (`P:799–811`; `.dagr/run-plan003.json` task inventory: `P1P1`–`P1P6` owner `claude·opus5`). REV1 is labelled owner `claude` although its title/criteria say Codex; gates/questions correctly say operator (`G:REV1,V5G,QRET,P1G,P3G`). Clarify whether owner means orchestrator or executing reviewer/delegate and encode the latter consistently.

The graph is **not yet usable as §8's durable `dagr_plan`**. D:638–644,668–690 says the producer READS the operator plan, never writes it; seeding is idempotent, and an existing task is left alone. §8 instead directs Motoko, the producer/orchestrator, to update `.dagr/run-plan004.json` after each settlement, including `generated_at` and receipts (`P:778–783,823–827`); this conflicts with the one-writer boundary. The extension's own run file receives attempts/settlement; the operator can maintain a separate plan input and reconcile from git, or a **single designated plan writer** can own a new input between producer sessions. §8 should say: pass `dagr_plan` and `dagr_task`, read the plan, never write it; collect receipts from the producer run/git and ask the operator to update the input when necessary. `PSYNC` must not edit already seeded definitions expecting deps/criteria to change in the producer: D:643–644 says additions seed, existing tasks do not update. Use new task IDs for changed parts with explicit cancellation/supersession of old ids, or start a fresh producer session on a reviewed v1.1 plan. If §8 retains a manual graph mirror, it must name a writer **other than the producer** and state that the mirror is not the producer's observed run file (`P:276–279,823–827`; `D:626–632,638–650`).

## §8 — Claim audit: factual errors and corrections

| Plan claim | Correction with evidence |
|---|---|
| “Nothing returned touches” P0/guard/variant/scan (`P:14–22`) | True only as *provisional* work. SCAN0 uses R's body16 heuristic and P1.1 changes what D5 later pins; neither may establish admission or protected baseline until v5 (`R:Required changes 6,8`; `P:185–204,293–294`). Qualify the universal wording. |
| “Every P1 part implementing D1/D3/D5 waits” (`P:23–24`) | P1.1 changes `ported_provider` and `stub_step` which D5 protects at A, but before A this is allowed. Say why A pins the post-variant source, rather than imply P1.1 is outside D5 (`A:796–804`; `P:291–293`). |
| `P:31` “strict-clean” | True syntactically: `dagr check ... --strict --json` → `[]`; it does not validate conditional review acceptance, seeding freshness or one-writer use (`D:638–644`; §7). |
| P0 `½–1 day` for preserve + heavy sweep + scan (`P:122,763–764`) | Internal estimates are `½+½+½` days (`P:149,176,185`) = 1½ delegate-days. One elapsed day is possible only with SWEEP/PRESERVE overlap; say whether the estimate is wall time or effort and allow sweep wall uncertainty. |
| ADR `2–3 days` including nine changes, review and PSYNC/PREV (`P:123,765`) | V5 1–2, V5R ½–1, PSYNC/PREV together 1 day (`P:206,255,274–281`) give 2½ days at their lower bounds. Two days is unattainable without overlap; three assumes one review round and no graph repair. Mark the estimate conditional. |
| `P:209–210` V5 “decide every ⟨v5⟩ marker in this plan” | ADR revision decides ADR questions; it should not edit PLAN-004 concurrently. PSYNC applies answers after accepted review (`P:26–27,274–279`; `D:640–644`). |
| `P:310` “all three files” | Exactly three distinct files are touched although five lines change (`P:296–303`; `wc -l` output §3). Correct, but gate must inspect `git diff --unified=0`, not rely on stat. |
| `P:489–496` manifest real provenance validated by `validate_manifest` | Validation checks presence/profile/version, not truth; actual derived helper takes caller-supplied revision/toolchain/config/sets (`dst_profile.ail:1535–1573`; `dst_driver_only.ail:1097–1123`). Add independent source/preflight comparison. |
| P1.9 exhausted marker as `ProgramExhausted` test (`P:602–605` by implication) | `ProgramExhausted` depends on expected-log exhaustion; first mismatch may be projection/outcome earlier (`dst_replay.ail:357–386`; `R:§3.4`). Test direct length mutation and marker finding separately. |
| P3.2 “basis equals A's by construction” (`P:697–699`) | A reverse `de4b4f5` source diff changes phase_vocab imports/function. Only protected pins may match, contingent on closure; assembled source identity differs. D3 intent and byte-identical raw inputs require review (`git show de4b4f5 -- src/core/phase_vocab.ail`; `A:665–676,803–804`). |
| P3.2 “it is the defect the evidence measured” (`P:697–699`) | Historical `de4b4f5` measurement was a 299-step/terminal-augmented prototype and contains an erroneous 1,756 digest tally; finite ≤100-call final-evaluator allocation regression is unmeasured (`git show --stat de4b4f5`; `R:§4.1,§3.11`; `P:682,701–702`). |
| P5 “300-call T0 replay peaked” (`P:738–740`; `G:P5 note`) | R:§4.1 corrects this as a historical terminal-augmented prototype, not a newly admitted T0 entry; restate it as scale only. |
| `P:93–95` one-commit exceptions complete | Review/gates/questions and P3.1/P5 record work are omitted; enumerate them (§6; `P:255–281,684–742`). |
| `P:143,793–796` ready set | Correct for five queued no-dep nodes; graph also has independent `REV1` working. `python3` graph inventory output §7. |
| §8 says graph can be refreshed by producer (`P:823–827`) | D:638–644,688–690 says plan input is read-only to producer and edits to seeded ids do not overwrite observations. Replace §8 protocol (§7). |

## Required changes for v1.1

1. Retain only provisional early tasks; state that SCAN0 is heuristic, P1.1's post-variant code becomes A's D5 pin basis, and no real admission or protected pins occur before accepted v5 (`P:14–27,185–204,291–293`; §1).
2. Add `REV1` to PSYNC's deps and block P1 implementation on accepted P1.1R/PREV verdicts and V5G's **latest** non-RETURN review. Make all correction loops, including `rounds_max` exhaustion, mechanically checkable (`G:REV1,V5R,PSYNC,PREV,P1.1R`; §7).
3. Rewrite §8 and PSYNC for §10.5's one-writer/idempotent-seeding contract. The producer only reads `dagr_plan`; a designated operator owns plan edits; changed seeded tasks get new ids/supersession or a fresh producer session. Verify the revised graph's effective deps/criteria before delegation (`P:276–279,823–827`; `D:638–644,668–690`).
4. Fix retention before any real-content copy, with duration and deletion rule for archive, scan0, entries, runs and backups; make QRET's ruling receipt a gate. Add SCAN0 exposure migration and P4/P5 held-out/agent criteria (`P:748–750`; `A:842–894`; §6–§7).
5. Expand the D8 P1 case→test matrix: paired refusal/divergence/clean boundaries by family, each first `ReplayMismatch`, exact cutoffs/branch shapes, complete failure `TimedOutcome`, A1–A9 and K0–K7 negative cases, env/profile faults, census assertions, scan components and tagged locations. Require real red-first evidence only where a pre-implementation test can fail (`A:926–930`; `P:288–289,614–620`; §5).
6. Specify P1.4 protected closure and checker parser/hash failure behavior against original source bytes, including stub/session/ports imports, declarations and helpers; specify P1.6 independent manifest identity derivation and P1.9 resolved-import/cache execution provenance (`P:404–430,473–496,573–586`; `R:Required changes 5–6`; §3–§4).
7. Have v5 expressly choose and review the P3.2 control class and basis. For the reverse `de4b4f5` option, show a diff admissible under D3, unchanged protected pins/canonical output, K1–K7 observation equality and a *prospective* finite-entry allocation-budget failure; do not count K0 or historical prototype numbers (`P:692–705`; `A:665–676,939–948`; §4).
8. Correct project estimates, no-code-commit exceptions, semantic citations and prototype attribution; give P2.2/P3.1/P4/P5/P6 gate criteria, and leave unscheduled P6 non-ready. Re-run `dagr check --strict --json` and audit graph↔plan after these edits (`P:118–127,93–95,733–742`; `G:P4,P5,P6`; §4,§7–§8).
