-- REJECTION: `hook-record-unresolvable`.  The hooks record is produced by a
-- function this closure does not hold, so the eight bindings cannot be located
-- at all.  Failing closed here is the whole difference between "no ambient
-- source found" and "nowhere looked".
module fixture/reject_record_unresolvable

import pkg/sunholo/motoko_ext_abi/types (ExtensionHooks)

export func register_with_config(_cfg: a) -> ExtensionHooks {
  make_hooks_from_somewhere_else(1)
}
