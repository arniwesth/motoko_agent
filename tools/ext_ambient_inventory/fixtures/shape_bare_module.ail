-- SHAPE 2/5: module import with no symbol list.
-- The same loss of granularity as the alias, reached by unqualified/computed
-- access instead.  Rejection.
module fixtures/shape_bare_module

import std/io

export func emit(s: string) -> () {
  ()
}
