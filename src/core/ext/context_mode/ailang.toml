[package]
name = "sunholo/motoko_context_mode"
version = "0.1.0"
edition = "1"
module_prefix = "src"

[exports]
modules = ["src/core/ext/context_mode/context_mode"]

[dependencies]
"sunholo/motoko_core" = { path = "../.." }

[effects]
max = ["Process", "FS", "IO", "Env", "SharedMem"]
