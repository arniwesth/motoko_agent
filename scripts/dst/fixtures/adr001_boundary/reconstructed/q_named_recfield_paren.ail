-- PLAN-001 (031) P0.2b. Reconstructed from shape, not quoted source.
-- Review 5 (REVIEW-adr001-v0.5-verdicts-claude-fable-5-1.md) §A.3 table row, verbatim:
-- | `q_named_recfield_paren` | same, applied as `(r.f)(…)` | 1 | — | rejected: app |
module scripts/dst/fixtures/adr001_boundary/reconstructed/q_named_recfield_paren
import std/io (println)
import scripts/dst/fixtures/adr001_boundary/types (PureCtx, Slot, Pure)
type R = { f: (string) -> () ! {} }
func body(ctx: PureCtx) -> int { let r: R = { f: println }; let _ = (r.f)(ctx.name); 1 }
func build() -> Slot { Pure(body) }
func invoke(s: Slot, ctx: PureCtx) -> int { match s { Pure(f) => f(ctx) } }
export func main() -> () ! {IO} { println("result=${show(invoke(build(), {name: "EFFECT WITH NO PORTS OR WORLD"}))}") }
