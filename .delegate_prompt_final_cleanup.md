You are working in the motoko_agent repository (workdir root: /workspaces/motoko_agent). Run all shell commands from /workspaces/motoko_agent. You are on branch `arniwesth/mot-129-extension-abi-phase-a` at commit `068c634` (current HEAD).

The main-task code work for the "final comment/cleanup pass" is ALREADY DONE and committed in two commits:
- `13c5fae` — "final cleanup: correct atoms_at_zero header, drop dead neutral helpers"
- `068c634` — "final cleanup: complete the dead neutral-helper drop (one remaining package)"

However, the mandated REPORT for that task was never written: `.motoko/herdr-delegates/answer-mot-dlg-1787828695261.md` is MISSING. Your job: verify the tree at HEAD is fully green and produce that missing report in the exact format the task requires.

STEP 0 — READ THE CONTEXT FIRST
1. Read `.motoko/herdr-delegates/task-mot-dlg-1787828695261.md` fully — the authoritative spec: it defines the two items (comment fix in `src/core/dst_profile_coverage.ail` + dead neutral-helper cleanup), scope constraints (comment/dead-code hygiene only, no behavior change, do NOT touch `.gitignore`/`ailang.lock`, no new files), the LOCKED decisions list, MANDATORY verification list, and exact "REPORT BACK" 5-section format.
2. Inspect what the two commits changed:
   - `git show --stat 13c5fae` and `git show --stat 068c634`
   - `git show 13c5fae -- src/core/dst_profile_coverage.ail` — capture exact stale "one atom per kind" header text BEFORE and corrected text AFTER (quote verbatim in report).
   - `git show 13c5fae` and `git show 068c634 -- <each packages file>` — list every top-level dead helper removed, per file.
3. Re-derive "dead" evidence: for each removed helper name, run a repo-wide search across packages/, src/, scripts/, tools/, examples/ to confirm ZERO remaining references. Record paths searched + zero hits. If a helper name still referenced anywhere, disclose and keep (do NOT delete anything — code is already committed; you only verify and report).

VERIFICATION — MANDATORY, capture rc + tail each. Sequential only; NEVER run heavy targets in parallel (use `DST_JOBS=1`, no concurrent heavy job). Run from /workspaces/motoko_agent.
- `make check_core` — expect `src/core/ type-check: 56 passed, 0 failed` and `verify_extensions (default): 9 booted, 0 failed`.
- `make verify_core` — expect `verify_core: 2 with contracts, 0 failed, 51 without contracts`.
- Profile/package gates: `make profile_coverage`, `make profile_definition`, `make driver_only`, `make driver_plus_no_ops`, `make driver_plus_compose`, `make conformance`, `make registry_multiplicity`, `make declared_vs_performed`, `make ext_ambient_inventory_selftest`, `make ext_hook_scope_selftest`, `make ext_call_inventory_selftest` — all rc 0.
- `make attribution_table` and `make anchors` — anchors must remain with a checkmark and NO anchor moved.
- `make dst DST_JOBS=1` — SEQUENTIAL (-j1) full sweep, expect rc 0, nothing Killed/137, and NO 'NOTE: ... is listed in DST_KNOWN_RED but PASSED' lines (DST_KNOWN_RED empty at this commit). Takes ~9-10 min.
- `git diff --check` clean.
- `git status --short` — expect only pre-existing dirt: modified `.gitignore`, `ailang.lock`, untracked `.agent/projects/...` and `.claude/skills/dagr-producer/`. These are KNOWN pre-existing and out of scope — do NOT touch/revert.

WRITE THE REPORT — to `.motoko/herdr-delegates/answer-mot-dlg-1787828695261.md` following EXACTLY the task's 5-section "REPORT BACK" format:
1. Comment fix: exact stale text → corrected text (verbatim), file + location.
2. Dead helpers removed, one bullet per helper: name, file, proof of no-references (paths searched + zero hits). Name any helper KEPT because a reference existed + referencing site.
3. Commit hash(es) + messages (13c5fae and 068c634).
4. Gate table: command → rc → tail (full sweep green + anchors no-move + attribution_table green).
5. Findings/deviations/residual risks, numbered style; disclose anything not cleanly verified.

Constraints: do NOT modify any source or Makefile (code already done). Do NOT create extra files. Do NOT touch `.gitignore`/`ailang.lock`. Report real rc/tail — never fabricate a gate result.

When done, reply with a short summary: report path, key gate results (rc each), deviations/unresolved issues. IMPORTANT: use the Claude Code tooling (ReadFile, BashExec with bash -lc, etc.) exactly as normal. Do not wrap commands in markdown fences when emitting tool calls.