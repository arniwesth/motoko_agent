# verifier-words — what blocks a contract on each excused declaration

GENERATED by `evidence/CONTRACTS-PROD/probe.py`. Two probes per declaration:

* **trivial** — the `ensures { true }` the policy checker synthesises. Rejection
  here is what makes the `-- contracts:` line pass its own gate.
* **honest property** — what the excuse says it wanted to state. This is the one
  worth reading: a rejected `true` shows the function is outside the fragment, but
  only this shows the real property was tried rather than assumed impossible.


## `vote_family`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED vote_family
    Reason: SMT declaration closure could not resolve a required sort (unresolvable SMT declaration types: sort "Record_key_value" referenced in declaration of "Json" is not declared)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

**honest property** — the four vote kinds fall into exactly two families

```ail
  ensures { result == None || result == Some("tool-policy") || result == Some("finalize") }
```

```
  ⚠ SKIPPED vote_family
    Reason: SMT declaration closure could not resolve a required sort (unresolvable SMT declaration types: sort "Record_key_value" referenced in declaration of "Json" is not declared)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

## `capability_data`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED capability_data
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: let value: let body: let value: unresolvable SMT declaration types: calls user function "js" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

**honest property** — every atom discloses at least its kind

```ail
  ensures { result != jo([]) }
```

```
  ⚠ SKIPPED capability_data
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: let value: let body: let value: unresolvable SMT declaration types: calls user function "js" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

## `strings_json`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ! ERROR strings_json
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

**honest property** — no advertised name is dropped or duplicated

```ail
  ensures { length(result) == length(xs) }
```

```
  ! ERROR strings_json
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

## `atoms_data`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ! ERROR atoms_data
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

**honest property** — one data position per atom, list order preserved

```ail
  ensures { length(result) == length(caps) }
```

```
  ! ERROR atoms_data
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

## `config_digest_material`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED config_digest_material
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: let value: unresolvable SMT declaration types: calls user function "kv" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

**honest property** — the material carries config AND the atoms (N58)

```ail
  ensures { result != jo([]) }
```

```
  ⚠ SKIPPED config_digest_material
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: let value: unresolvable SMT declaration types: calls user function "kv" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

## `compare_bytes`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED compare_bytes
    Reason: Function "compare_bytes" uses an unencodable builtin: std/string.charAt
    Hint: Z3 has no SMT-LIB encoding for std/string.charAt. Either remove its use, refactor to use a supported builtin, or narrow the function's contracts.
```

**honest property** — the ordering's three-valued range

```ail
  ensures { result == -1 || result == 0 || result == 1 }
```

```
  ⚠ SKIPPED compare_bytes
    Reason: Function "compare_bytes" uses an unencodable builtin: std/string.charAt
    Hint: Z3 has no SMT-LIB encoding for std/string.charAt. Either remove its use, refactor to use a supported builtin, or narrow the function's contracts.
```

## `insert_kv`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ! ERROR insert_kv
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

**honest property** — insertion adds one key and drops none

```ail
  ensures { length(result) == length(sorted) + 1 }
```

```
  ! ERROR insert_kv
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

## `sort_kvs`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ! ERROR sort_kvs
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

**honest property** — the sort is a permutation, not a filter

```ail
  ensures { length(result) == length(kvs) }
```

```
  ! ERROR sort_kvs
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

## `canonical_kvs`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ! ERROR canonical_kvs
    encoding error: resolving cross-function calls: circular function call detected: canonical_list → canonical → canonical_list
```

**honest property** — recursing into values loses no pair

```ail
  ensures { length(result) == length(kvs) }
```

```
  ! ERROR canonical_kvs
    encoding error: resolving cross-function calls: circular function call detected: canonical_list → canonical → canonical_list
```

## `canonical_list`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ! ERROR canonical_list
    encoding error: resolving cross-function calls: circular function call detected: canonical → canonical_kvs → canonical
```

**honest property** — array order is preserved (must NOT sort)

```ail
  ensures { length(result) == length(xs) }
```

```
  ! ERROR canonical_list
    encoding error: resolving cross-function calls: circular function call detected: canonical → canonical_kvs → canonical
```

## `canonical`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED canonical
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: match body: let value: unresolvable SMT declaration types: calls user function "canonical_kvs" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

**honest property** — canonicalisation changes key order and nothing else

```ail
  ensures { result != JNull }
```

```
  ⚠ SKIPPED canonical
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: match body: let value: unresolvable SMT declaration types: calls user function "canonical_kvs" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

## `canonical_json`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ! ERROR canonical_json
    encoding error: resolving cross-function calls: circular function call detected: canonical → canonical_kvs → canonical
```

**honest property** — deterministic for equal inputs (a TWO-call property)

```ail
  ensures { result != "" }
```

```
  ! ERROR canonical_json
    encoding error: resolving cross-function calls: circular function call detected: canonical_list → canonical → canonical_kvs → canonical
```

## `ext_config_digest`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED ext_config_digest
    Reason: Function "ext_config_digest" uses an unencodable builtin: show
    Hint: Z3 has no SMT-LIB encoding for show. Either remove its use, refactor to use a supported builtin, or narrow the function's contracts.
```

**honest property** — depends on config AND the data positions (N58); also two-call

```ail
  ensures { result != "" }
```

```
  ⚠ SKIPPED ext_config_digest
    Reason: Function "ext_config_digest" uses an unencodable builtin: show
    Hint: Z3 has no SMT-LIB encoding for show. Either remove its use, refactor to use a supported builtin, or narrow the function's contracts.
```

## `ext_config_digests`

`src/core/ext/registry_normalize.ail`

**trivial** — `ensures { true }`

```
  ! ERROR ext_config_digests
    encoding error: resolving cross-function calls: circular function call detected: canonical_json → canonical → canonical_kvs → canonical
```

**honest property** — one row per entry, registry order

```ail
  ensures { length(result) == length(entries) }
```

```
  ! ERROR ext_config_digests
    encoding error: resolving cross-function calls: circular function call detected: canonical_json → canonical → canonical_kvs → canonical
```

## `entry_base`

`src/core/ext/runtime.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED entry_base
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: let body: let value: unresolvable SMT declaration types: calls user function "pure_ctx_of" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

**honest property** — D3: other hook sites receive None

```ail
  ensures { result.decision_state == None }
```

```
  ⚠ SKIPPED entry_base
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: let body: let value: unresolvable SMT declaration types: calls user function "pure_ctx_of" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

## `stub_decision_answer`

`src/core/ext/runtime.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED stub_decision_answer
    Reason: Function "stub_decision_answer" calls "stub_usage" whose signature uses an unencodable type "()"
    Hint: Cross-function verification cannot encode parametric ADTs (Option/Result) or type variables in a callee signature. Callees returning records/enum ADTs are also not yet inlinable and will skip. Rewrite the callee to return a primitive, or inline its logic into the caller.
```

**honest property** — the unconfigured arm bills nothing

```ail
  ensures { result.usage.cost_millicents == Some(0) }
```

```
  ⚠ SKIPPED stub_decision_answer
    Reason: Function "stub_decision_answer" calls "stub_usage" whose signature uses an unencodable type "()"
    Hint: Cross-function verification cannot encode parametric ADTs (Option/Result) or type variables in a callee signature. Callees returning records/enum ADTs are also not yet inlinable and will skip. Rewrite the callee to return a primitive, or inline its logic into the caller.
```

## `decision_invocation_state`

`src/core/ext/runtime.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED decision_invocation_state
    Reason: Function "decision_invocation_state" uses an unencodable builtin: show
    Hint: Z3 has no SMT-LIB encoding for show. Either remove its use, refactor to use a supported builtin, or narrow the function's contracts.
```

**honest property** — a fresh state has spent nothing and claims no cap

```ail
  ensures { result.interventions_used == 0 && result.run_cap_millicents == None }
```

```
  ⚠ SKIPPED decision_invocation_state
    Reason: Function "decision_invocation_state" uses an unencodable builtin: show
    Hint: Z3 has no SMT-LIB encoding for show. Either remove its use, refactor to use a supported builtin, or narrow the function's contracts.
```

## `decision_view`

`src/core/ext/runtime.ail`

**trivial** — `ensures { true }`

```
  ⚠ SKIPPED decision_view
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: let value: unresolvable SMT declaration types: calls user function "decision_invocation_state" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

**honest property** — the cursor is the only site that stamps one

```ail
  ensures { result.decision_state != None }
```

```
  ⚠ SKIPPED decision_view
    Reason: SMT declaration closure could not resolve a required sort (cannot encode function body: let value: unresolvable SMT declaration types: calls user function "decision_invocation_state" that is not SMT-encodable in this context)
    Hint: The function uses a type shape whose required SMT sorts cannot be declared
```

## `count_purity`

`src/core/dst_profile_coverage.ail`

**trivial** — `ensures { true }`

```
  ! ERROR count_purity
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

**honest property** — a count is bounded by what it counts

```ail
  ensures { result >= 0 && result <= length(ks) }
```

```
  ! ERROR count_purity
    encoding error: unrolling recursive function: encoding unrolled body at depth 1: match pattern: unsupported pattern type *core.ListPattern in SMT encoding
```

## `empty_tool_evidence`

`src/core/ext/ctx_defaults.ail`

**trivial** — `ensures { true }`

```
  ! ERROR empty_tool_evidence
    Z3 error (exit exit status 1): (error "line 8 column 119: unknown constant mk_ToolEvidenceWindow (Bool Option (Seq Int))
declared: (declare-fun mk_ToolEvidenceWindow
             (Bool Option (Seq ToolEvidence))
```

**honest property** — the window asserts no success

```ail
  ensures { not result.complete_from_session_start }
```

```
  ! ERROR empty_tool_evidence
    Z3 error (exit exit status 1): (error "line 8 column 119: unknown constant mk_ToolEvidenceWindow (Bool Option (Seq Int))
declared: (declare-fun mk_ToolEvidenceWindow
             (Bool Option (Seq ToolEvidence))
```
